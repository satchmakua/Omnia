# PROGRESS.md — Session Log

Append a new entry at the **TOP** after every session (reverse-chronological, newest first). Be honest, including dead ends — a future session may be a fresh instance with no memory of this one. Keep entries short.

**Entry template:**

```
## Session N — <date> — <one-line summary>
- Did: <what changed>
- State: <where things stand; is the build green?>
- Next: <the next item to pick from ROADMAP>
- Blockers/Questions: <none, or the question>
- Decisions logged: <DECISIONS.md ids, or none>
```

---

## Session 68 — 2026-06-25 — M18 slice 2: faith spreads (conversion) + devotion comforts → M18 DoD met
- Did: Finished M18's "spreads" + "affects behaviour" clauses. **(1) Conversion / spread by contact.** A throttled daily adjacency pass in `ReligionSystem`: a folk standing beside a **more-devout** faith may **adopt it** (`conversionChancePerDay`, one roll/folk/day → bounded) — so faith spreads by persuasion, not only by birth, and **devout faiths grow while lax ones wither**. Founding faiths now carry a real **spread of devotion** (fervor `0.35 + traditional·0.5 ± 0.18`) so conversion has direction. Bounded by the schism counter-pressure (no monoculture). **(2) Devotion comforts (faith→mood).** `MoodSystem` lifts a follower's mood target by `FAITH_COMFORT·fervor` — a devout faith is a solace (and, via the mood→social coupling, the comforted bond more). Both are causal (D26), legible (conversion "X converted to the Faith of Y" in the feed).
- State: **Green.** 457 tests (was 454; +3 `test/religion.test.ts`: a folk beside a more-devout faith converts; holds firm beside a less-devout one; a devout-faith follower ends in a better mood than the faithless). `tsc` clean. **40k soak: 0 violations, pop stable 60, faiths bounded** (form 2 → schism to 3; conversion doesn't collapse them to one). Determinism holds (conversion draws RNG → trajectory shifts but replays per seed; properties/saveload green). **Verified** the mechanic with a headless A/B: faith-changes over 40k ticks were **21 with conversion OFF vs 30 ON** (the +9 are conversions; schism accounts for the rest), and the faith **shares** show the effect (a devout faith 0.73 dominant at 40 followers while a lax 0.44 withers 9→2). **Live-verified** (preview, seed 8, no console errors): "Ninos Supu converted to the Faith of Drugro" in the feed; the town's devout-faith followers sit at mean mood 0.888.
- **M18 DoD met (S67–S68):** faiths found from culture, **spread** (birth + conversion), and **schism** over deep time tied to culture; faith **visibly affects behaviour** (bonding + mood) **and events** (conversions/schisms in feed & Chronicle); **legible** (Faiths tab R + inspector); determinism + soak hold.
- Next: **Milestone 19 — Events, Seasons & the Paranormal** (a content-driven event pipeline; seasons; disasters — famine/plague/quake; rare paranormal events; an events/timeline view). *(Deferred M18 flavour: founding myths, holidays, cults, living/embodied gods + divine effects.)*
- Blockers/Questions: none. Note: conversion is intentionally sparse (contact + more-devout gate) — the visible "spread" is in the shifting faith *shares*, not a flood of feed lines; `conversionChancePerDay` is the dial.
- Decisions logged: none new (conversion is a D26 contact coupling like fluency-learning; faith→mood is the same circumstance→mood pattern as homes/family/debt).

## Session 67 — 2026-06-25 — M18 slice 1: the religion engine — faiths that found, are inherited & schism
- Did: Opened **Milestone 18 (Religion & Belief)** by mirroring the proven tribe/culture engines (D12/D33). New **`src/religion/religionStore.ts`** — a singleton of `Religion` records (name, language-coined **deity**, **tenets**, **fervor**/devoutness, cohesion, descent, violet-based colour) that agents follow by **`religionId`** (new on `Agent`). **World-gen** (`seedReligions`) **founds one faith per seed culture** — its tenets & fervor emerge from the culture's values (communal→communal worship, martial→the warrior creed, traditional→ancestor rites & higher devotion); founders take their culture's faith and **children inherit the mother's** (`spawnAgent`/`ReproductionSystem`). New **`ReligionSystem`** (daily, schism on the era cadence): **extinction** (a faith with no followers falls), and **schism** — a large, loosely-held faith fractures into a **sect** (`forkReligion` — new deity/name, nudged fervor, descent link) that gathers half the faithful; fervor also drifts each era. **Causal coupling (D26):** a shared faith **warms bonds** — `faithFactor` multiplies SocialSystem warmth (co-religionists draw closer, scaled by the faith's fervor; the differently-faithed a touch cooler; neutral for the unbelieving). **Legible:** a new **Faiths master tab** (hotkey **R**, `faithsDashboard.ts`) — each faith's deity, tenets, devoutness, follower count, descent + forgotten faiths — and a per-agent **Faith** line in the inspector. Two config knobs (`religionSchismChancePerEra`, `minFaithFollowers`).
- State: **Green.** 454 tests (was 448; +6 `test/religion.test.ts`: faithFactor same/diff/absent, forkReligion descent+tenets, prune, ReligionSystem extinction + schism-splits-the-faithful, world-gen founders get a faith). `tsc` clean. **40k soak: 0 violations, pop stable 60** — faiths form (3) and **schism to 4** over the run; determinism holds (seeding/schism draw RNG + faithFactor shifts social warmth → trajectory shifts but replays per seed; properties/saveload green). **Live-verified** (preview, seed 8, ~26k ticks, no console errors): 5 living faiths incl. 3 sects (the Faith of Drugro fervor 0.77 → the Order of Dogrob fervor 0.83, …); the Faiths tab renders; the inspector reads "FAITH ● the Faith of Drugro · venerates Drugro · personal devotion, the warrior creed, ancestor rites".
- Next: **M18 remaining** — **conversion/spread** through contact (faith currently spreads only by birth), **founding myths**, and **holidays / cults / living gods / divine effects**; then the M18 DoD is met.
- Blockers/Questions: none. Note: faith is the 5th evolving lineage store (cultures/tongues/tribes/now faiths) — all reuse the same fork/schism/prune machinery, which keeps the engine count low (D12 tenability).
- Decisions logged: none new (religion is the D33 Organization model applied to faith — a store of records forked on schism — and the faith→bonding coupling is D26, like culture's bondFactor).

## Session 66 — 2026-06-25 — M17 slice 4: lost tech / rediscovery + achievements → M17 DoD met
- Did: Finished M17's last two clauses. **(1) Lost tech & rediscovery.** The OrgStore now tracks `everKnown` (every tech any tribe ever unlocked) vs what *living* tribes currently hold (`ResearchSystem`, daily): when the last holder of an art dies out, it's **lost** — "The art of X has been lost" (feed + a dark-age Chronicle legend); when a tribe re-researches it, **rediscovered**. (Rare in normal play — schism propagates knowledge — but real: a unique-tech tribe dying triggers it; tested.) **(2) Achievements.** New singleton `Achievements` + `AchievementSystem` (daily) fires civ + agent milestones **once**: Reach the Bronze/Iron/Medieval/Industrial/Modern/**Sci-Fi** Age, The First War, First Blood, Bloodshed (a murder), Archmage (mastery 5), Venerable Elder (age 100), A Lost Art — each a 🏆 feed line + Chronicle legend, **viewable** in the Legends view (a new Achievements section).
- State: **Green.** 448 tests (was 446; +2 `test/tech.test.ts`: an art is lost when its holders die out & rediscovered on re-research; an achievement fires once and not twice). `tsc` clean. **40k soak: 0 violations, pop stable 60, byte-identical to S65** (lost-tech/achievements are pure reads/bookkeeping — no RNG). **Live-verified** (preview, seed 8, ~30k ticks, no console errors): the Legends view lists a real civ timeline — "🏆 Reach the Bronze Age — Rkkharur clan (yr 3) · The First War (yr 5) · First Blood — Enlikes Kha-kar (yr 5) · Iron Age (yr 7) · Medieval (yr 13) · Industrial (yr 22) · Archmage — Unlevse Seis (yr 25)".
- **M17 DoD met (S63–S66):** a visible **tech tree** (climbs Modern by yr 38 / Sci-Fi by ~93) **and** a **magic tree** (schools + spell ladder, inspector mage sheet); tech & magic are **causal** (arms/medicine bonuses; mages blast/heal/feed/hearten); **mages inspectable & useful**; **knowledge lost & rediscovered**; **achievements fire & are viewable**; determinism + soak hold.
- Next: **Milestone 18 — Religion & Belief** (religions emerge from culture/values + founding myths; schism alongside culture/language/tribe via the Organization engine; faith biases behaviour; a religion view).
- Blockers/Questions: none. Deferred (ROADMAP): magic items/golems, resource→craft→goods + skill gating, a dedicated Knowledge-tree tab, content-ifying the magic schools.
- Decisions logged: none new (lost-tech reuses the D12 compression ethos; achievements are derived from durable state, D22-style — no coupling, no RNG).

## Session 65 — 2026-06-25 — M17 slice 3: the magic tree + legible mages (what mages actually do)
- Did: Answered the "what do mages do?" ask (D35). New **magic-school tree** (`src/magic/schools.ts`): four schools — **elementalism / restoration / divination / conjuration** — each a ladder of named spells gated by **mastery**. `Magic` gained `school` + `mastery`; mages **roll a school at birth** (mastery scaled by age — a seasoned founder is already adept) and mastery **grows over a life**. New **`MagicSystem`** (after combat): a mage casts its school's signature spell on a neighbour when mana allows — **elementalists blast adjacent beasts** (and earn the slaying — battle-casters in the Conflict champions), **restorers mend the wounded**, **conjurers feed the hungry**, **diviners hearten the downcast**; the slow mana regen keeps it occasional. **Mages made legible:** the inspector now shows a full **mage sheet** — the school, mastery, mana, and the **spell tree** (✦ known / ○ locked, with the mastery each needs) — so you can read exactly what a mage commands.
- State: **Green.** 446 tests (was 440; +6 in `test/magic.test.ts`: 4 schools + mastery gates spells; an elementalist slays an adjacent beast & earns the kill; a restorer heals a wounded neighbour; no-mana → no cast; mastery grows on a day boundary; world-gen mages get a school+mastery). `tsc` clean. **40k soak: 0 violations, pop stable 60** (the school roll adds one RNG draw at spawn → trajectory shifts but replays per seed; properties/saveload green). **Live-verified** (preview, seed 8, no console errors): an elementalist "Otat Usot · mastery 3" inspects with "MAGIC ✦ ELEMENTALISM · MASTERY 3 · Mana 10% · ✦ Spark · ✦ Flame Lash · ○ Storm Wrath (m5)" — a visible magic tree; schools spread across mages (elementalism m3.2, restoration m2.2).
- Next: **M17 slice 4 — lost tech / rediscovery + achievements** (the last two DoD clauses), then M17 is done → **M18 Religion**.
- Blockers/Questions: none. Deferred: magic items & golems (need an inventory + a stronger advanced-mage payoff), enemy-folk bolting in war, and content-ifying the schools (currently a code table).
- Decisions logged: none new (executes D34's "magic tree + legible mages" via the org-less per-mage progression — magic aptitude is per-agent, D17, so schools/mastery live on the `Magic` component, not the Organization).

## Session 64 — 2026-06-25 — M17 slice 2: tech effects — research now matters (armed tribes fight & heal better)
- Did: Made the tech tree **mechanically real**. Techs carry **`effects` tags** in content (D9); the `ResearchSystem`, on unlock, **denormalises** them onto the tribe as effect levels (`org.effects`, e.g. `{arms:3, medicine:1}`), read cheaply by the hot systems (D51). Two effects wired: **`arms`** (bronze_working / iron_working / engineering / machining) raises a tribe's members' **hit chance & damage** in the combat engine — an armed war-band beats a tribal one in war, and armed folk slay beasts far more readily (a defender's armour resists, so equal arms cancel); **`medicine`** (scholarship / chemistry) speeds **recovery** in `HealthSystem` (advanced tribes shrug off illness). Effects are **inherited on schism**, and **legible** in the Tribes tab ("⚙ Industrial Age · 10 techs · ⚔ arms 3 · ⚕ med 1"). Recorded **D51**.
- State: **Green.** 440 tests (was 436; +4 `test/tech.test.ts`: arms raises hitChance/hitDamage & equal arms cancel, research accumulates `arms` from metalworking, a schism daughter inherits effect levels, and medicine speeds a HealthSystem recovery vs a no-medicine tribe). `tsc` clean. **40k soak: 0 violations, pop stable 60** — the effects shift the trajectory as intended: combat is more decisive (**kills 27→75, scars 1→23, graves 14→20**) as tribes arm up, fauna still 130, debt/Gini bounded. Determinism holds (no RNG; the changed combat/health *outcomes* shift the trajectory but replay per seed — properties + saveload green). **Live-verified** (preview, seed 8, ~24k ticks, no console errors): the Tribes tab shows emergent tech-power variation — Rkkharur clan T5/arms 3/med 1 vs Rkthuru clan T3/arms 2/med 0.
- Next: **M17 remaining** — the **magic tree + schools + legible mages** (the explicit "what do mages do?" ask, D35), then **lost-tech / rediscovery + achievements**; plus skill/knowledge gating, resource→craft→goods, and a dedicated **Knowledge tab**.
- Blockers/Questions: none. Note: arms makes combat noticeably more lethal (intended — advanced tribes dominate); pop/fauna stay bounded, but if it ever felt too swingy the `arms` damage factor (0.1/level) is the dial.
- Decisions logged: **D51** (tech effects = content tags denormalised onto the tribe as effect levels, read by the hot combat/health systems; bounded, inherited on schism).

## Session 63 — 2026-06-25 — M17 slice 1: the tech tree — tribes research & climb the ages (tribal → sci-fi)
- Did: Opened **Milestone 17 (Knowledge)** with its keystone — a content-driven tech ladder organizations climb by research (D34). New **`tech` content type** (`src/content/schema.ts` `TechSchema` + loader wiring + a referential-integrity check that prerequisites are real, earlier-tier techs) with **14 nodes across 7 tiers** in `content/tech/` (toolmaking/firecraft → … → computing/fusion_power; tribal/bronze/iron/medieval/industrial/modern/**sci-fi**), framed as re-ascending the fallen world's lost tech (D8). Each **tribe** (M14 `Organization`) gained `research`/`techs`/`tier`; a daily **`ResearchSystem`** accrues research (`researchBasePerDay` + `researchPerMemberPerDay` × members — bigger tribes advance faster) and unlocks the **cheapest prerequisite-met tech** it can afford, climbing the ladder. Each discovery is a feed line ("The X clan mastered Iron-working") and, for tier ≥ 3, a Chronicle **"reached the {Age}"** legend. On **schism**, the breakaway faction **inherits the tribe's techs + tier** (knowledge carries). Pure accumulation — **no RNG**. Legible: the **Tribes tab** shows each tribe's **era + tech count** (so you see one clan at Industrial while another lags at Iron). Soak prints `tech=T{maxTier}/{maxTechs}`.
- State: **Green.** 436 tests (was 430; +6 `test/tech.test.ts`: the ladder loads tribal→sci-fi with valid prerequisites, ResearchSystem unlocks cheapest-first in dependency order, a memberless tribe does nothing, a big tribe climbs ≥ tier 5 over 400 days, a schism daughter inherits techs+tier, and a live-loop integration). `tsc` clean. **40k soak: 0 violations, pop stable 60**, and the trajectory is **byte-identical** to S62 (research consumes no RNG) — the leading tribe climbs **Tribal → Modern Age (T6, 11/14 techs)** over 42 years. **Deep-time check (seed 8): Sci-Fi (T7) reached by yr ~93** — the DoD's full ladder. **Live-verified** (preview, no console errors): the Tribes tab shows eras (Rkkharur at Industrial T5/10, Avse at Iron T3/6 — emergent variation), the feed shows "The Drkurrko clan mastered Scholarship". **Bonus fix:** schism daughters were inheriting techs but not the `tier` (showed "T1 / 6 techs"); `forkOrg` now copies the tier too.
- Next: **M17 slice 2** — wire tech **effects** (effect-tags → tribe-wide combat/economy/farming bonuses; the schema already carries `effects`), add the **magic tree + schools + legible mages**, then **lost-tech / rediscovery + achievements**. Also a dedicated **Knowledge tab** with the tree.
- Blockers/Questions: none. Note: research is currently linear in members; INT/scholarship-driven research speed and tech *effects* are slice 2. Tech is per-tribe, so towns show a mix of advancement (intended).
- Decisions logged: none new — executes **D34** (content-driven tech tree climbed by Organizations via research) exactly as specified; the per-tribe research + schism-inheritance follows the D33/D43 org model.

## Session 62 — 2026-06-25 — Fixes: honest cause-of-death, a richer Conflict tab, idle + smooth movement
- Did: Three requested fixes. **(1) Cause of death — fixed "always misfortune" + made causes distinct & logical.** Root cause: the age-mortality ramp (`ageRatio^10`) kills folk across ~70–95% of lifespan, but the label only said "old age" past **90%**, so the many age-ramp deaths were mislabelled "misfortune" (and dwarves, lifespan 180–260, never hit 90% in a run). Now `HealthSystem` attributes each death to its **dominant hazard** — sick → "illness", age-ramp-dominant & past mid-life → "old age", else → "an accident" (renamed from misfortune). Wired **starvation** as a proper death too: `HungerSystem` now uses `killAgent` (cause "starvation", leaves a tombstone so lineage resolves) instead of a silent `destroyEntity`. Added a **`slayer`** field to `Tombstone` (threaded through `killAgent`) so violent deaths show "killed by X" while the histogram keeps a bounded category key. **Result (80k run): `old age 43, an accident 7, illness 1, fell in battle 1`** (was `misfortune 96`). **(2) Conflict tab — much richer:** a bounded **`warLog`** on the OrgStore records concluded wars; the tab now shows **War history** (who fought whom + peace/conquest + year), **Most combative / Most peaceful tribe** (by wars, kills, martiality), **Who killed whom** (recent slayings from the `slayer` tombstones), alongside the existing active wars / champions / most-wanted / cause-of-death. **(3) Movement — idle + smooth.** Folk now **linger** when aimlessly wandering (`wanderIdleChance` 0.6, via reinterpreting the single wander rng draw — fauna & blocked-fallback stay byte-identical). And the renderer **glides** mobile creatures between tiles at an interpolation alpha (the loop's inter-tick fraction), updated per tick via `renderer.syncPositions`, **only at watchable speeds (≤50 t/s)**, snapping on >1-tile jumps. Pure render — the sim still steps one tile/tick and replays identically (D50).
- State: **Green.** 430 tests (updated the HungerSystem test to the new tombstone semantics — a starved agent is no longer a living agent but leaves a "starvation" tombstone). `tsc` clean. **40k soak: 0 violations, pop stable 60** (the idle shifts the folk trajectory but replays per seed — properties/saveload green; fauna byte-identical since their wander idleChance is 0). **Live-verified** (preview, seed 8, no console errors): the Conflict tab shows "How the dead died: old age 11, an accident 4" (distinct, not misfortune), war history ("Drkurrko clan vs Rkkharur clan — Rkkharur clan prevailed · yr 35"), most-combative (Dradrka clan, 3 wars/10 kills/martial 0.85) & most-peaceful (Ketap clan, 0 wars); the interpolation glides a mover from tile 29→30 as **29.5 at alpha 0.5**, and 47/190 creatures linger in place per tick.
- Next: **Milestone 17 — Knowledge: Tech Tree, Magic & Research.**
- Blockers/Questions: none. Notes: "Who killed whom" is empty until an agent-vs-agent kill occurs (folk-vs-beast kills don't name a slayer; murders/battle-deaths do); if folk still feel too restless, raise `wanderIdleChance`. The screenshot tool still can't capture the canvas (rAF never idles) — smoothing verified by the fractional draw position.
- Decisions logged: **D50** (smooth motion by render interpolation, sim stays discrete; wander idle via a reinterpreted single rng draw; + the cause-of-death attribution & starvation-tombstone fixes).

## Session 61 — 2026-06-25 — Combat legibility: on-map fight FX, wounded pips & a Conflict tab
- Did: Made combat **visible by watching** (requested). Three additions, all render-only / near-zero sim cost (D49): **(1) On-map combat flashes** — the conflict systems now tag their EventLog entries with the **map position** of the event (an optional `{x,y}` on `EventEntry` via `emitEvent(…, pos?)`; positions captured before `killAgent` strips them). The **renderer** ingests newly-positioned combat events each frame into a small bounded list of **fading clash/death marks** (yellow clash spark for a blow, orange for crime, a bigger red ring + burst for a death), aged by real elapsed-ms so they fade & survive pause, reset on a new run. The sim is untouched (positions don't feed RNG → soak byte-identical). **(2) Wounded pip** — a folk with low `Health.value` (<0.55) shows a small red bar at the feet, so you see who's hurt (pure render). **(3) A new Conflict tab** (`conflictDashboard.ts`, hotkey **X**, registered as the 10th master tab): **active wars** (tribe ⚔ tribe + sizes), **Champions** (folk by foes-slain), **Most wanted** (outlaws by rap sheet), and **How the dead died** (the cumulative cause-of-death histogram as red=violent / blue=natural bars, with "N of M by violence"). Added the X key to the in-app Controls map.
- State: **Green.** 430 tests (no new tests — render/UI changes aren't import-tested; the EventLog position field is optional so existing tests are unaffected), `tsc` clean, **40k soak byte-identical** (position-tagging is behaviour-neutral). **Live-verified** (preview, no console errors): the Conflict tab renders real data (seed 8: the two-front Rkkharur war vs Rkthuru & Mipkor, a champions list, "0 of 7 by violence"); on seed 21 (combat-heavy), **24 positioned combat events were ingested into 24 on-map flashes** (illness/crime/work kinds, e.g. "Upros Oson was mauled by a pallid stalker" at (3,47)). (Canvas pixels couldn't be screenshotted — the rAF loop never idles for a capture, the known artifact — but the FX ingest/draw path runs clean and the DOM evals confirm the data.)
- Next: **Milestone 17 — Knowledge: Tech Tree, Magic & Research.**
- Blockers/Questions: none. Further legibility ideas noted as options (not built): a predator-threat glyph when a beast stalks folk; veteran/outlaw map badges; a war-front tint on clashing tribes; an animated health bar.
- Decisions logged: **D49** (on-map combat FX are render-only, driven by position-tagged events; a reusable pattern for any future on-map effect — spells/weather/disasters).

## Session 60 — 2026-06-25 — Post-combat stress test (4 scenarios) + a real leak fix it surfaced
- Did: A dedicated stress pass after the M16 combat integration — ran many headless instances well past the standard soak and checked extra combat-specific invariants (health stays in [0,1], no `Combat`/`Crime` component outlives its agent, the cause-of-death histogram keeps a small key-set). **It surfaced one real bug I'd introduced:** combat death causes embedded agent names (`murdered by {name}`, `killed defending against {name}`), and `WorldStats.causeOfDeath` is a per-cause histogram — so every distinct murderer/victim created a **new key**, growing that map unboundedly over deep time. **Fixed** by making those causes **fixed categories** (`'murdered'`, `'killed while attacking'`) — the names still appear in the feed/legends (bounded ring buffer / compressed eras). **Four scenarios, all PASS:** **(A) 8-seed robustness** (40k each) — every seed stable at pop 59–60, 0 violations, cause-keys 1–3; wide variety (seed 21 a town of monster-slayers k=98; seed 42 crime-heavy t=63). **(B) deep time** (150k ticks ≈ 156 yrs) — 0 violations, pop stable, **cause-keys held at 3** (the fix), eras capped 8 / samples 80; graves grow ~linearly to 101 (pre-existing: tombstones aren't pruned — negligible but O(deaths), noted to backlog). **(C) extreme violence** (10× predators, ~7× crime, constant war) — **no crash / violation / leak** even pushed toward collapse; the town **survives** (reproduction outpaces the violence, pop 13→60); murders/battles/predator deaths all fire, cause-keys bounded 5–7. **(D) scale + determinism + snapshot** — 200×200 big map with combat runs **3.46 ms/tick (~289 ticks/s)** within budget; **same seed replays byte-identical** despite all the new combat RNG (seed 9 differs); **snapshot save/load round-trips** the Combat/Crime components + `OrgStore.wars` and **continues in perfect lockstep**.
- State: **Green.** 430 tests, `tsc` clean, all four stress scenarios PASS. The only code change is the two fixed cause-strings in `CrimeSystem` (behaviour-neutral label change — no RNG/trajectory impact; determinism + suite unaffected). Stress scripts were throwaway (removed).
- Next: **Milestone 17 — Knowledge: Tech Tree, Magic & Research.**
- Blockers/Questions: none. **Observation (to backlog):** combat is very survivable for folk at default tuning (150k ticks: folk slew 224 beasts but suffered only ~2 battle-deaths, 0 predator/murder deaths) — the drama is in wounds/veterans/legends, not folk deaths; violent settings make it lethal and the town still holds. Tombstone growth + optional combat-lethality tuning are recorded in the backlog.
- Decisions logged: none new (the cause-key fix is a correctness fix enforcing WorldStats' existing "fixed key-set" contract).

## Session 59 — 2026-06-25 — M16 slice 3: war between tribes → M16 DoD met
- Did: Closed **Milestone 16** with war. New **`War` state on the OrgStore** (`wars: {a,b,since}[]` + `areAtWar`/`declareWar`/`endWar`). **`OrgSystem`** (era cadence, beside schism): a **martial** tribe (`values.martial ≥ warMartialThreshold`) of decent size **declares war** on another (→ a Chronicle legend); a war **ends** in **peace** (exhausted after `warDurationEras`) or **conquest** (one side routed below 40% of the other), each a legend. **`CombatSystem`** gained a **battle pass** — members of warring tribes who are **adjacent** trade blows via the shared combat engine (the first **agent-vs-agent** combat), casualties "fall in battle" (a legend; the slayer a veteran). Legible: the Tribes dashboard shows "⚔ at war with X". Recorded **D48**. (Also restructured CombatSystem so the war pass runs even with no predators, and guarded the battle loop against an entity that fell earlier in the same pass.)
- State: **Green.** 430 tests (was 423; +7 `test/war.test.ts`: war helpers symmetric/idempotent, two martial tribes go to war on the era cadence, a war ends in peace when exhausted, a peaceful pair never wars, adjacent enemies wound each other, kin/non-enemies don't fight). `tsc` clean. **40k soak: 0 violations**, **pop stable at 60, fauna 130**, **1–2 wars active** at a time (declared, fought, ended), kills climbing (beasts + battles, 38 by yr38), crime + combat all coexisting. Determinism holds (war/battles draw RNG → trajectory shifts but replays per seed; properties + saveload green). **Live-verified** (preview, seed 8, ~16k ticks, no console errors): two active wars incl. the Rkkharur clan fighting **on two fronts** (vs Rkthuru and Mipkor); Chronicle legends "The Rkkharur clan went to war with the Rkthuru clan"; the Tribes dashboard shows "at war with".
- **M16 DoD met (S57–S59):** agents fight (combat engine) / hunt (M8) / commit alignment-consistent crimes (theft/assault/murder); wounds persist & show (scars, veterans, outlaws); organized conflict emerges & is recorded as legends (tribes declare war, battle, make peace/conquer); legible; determinism + soak hold.
- Next: **Milestone 17 — Knowledge: Tech Tree, Magic & Research** (civilizations climb tribal→…→sci-fi; research by Organizations unlocks capabilities; magic schools; lost tech/rediscovery; achievements). Also folds in M15's deferred resource→craft→goods + skill gating.
- Blockers/Questions: none. Deferred (ROADMAP): weapons/armour items (await inventory), dismemberment/poison, prisons/slavery/drugs/brothels, gangs/organized vice, military hierarchy, war-over-territory (needs a territory model — tribes are intermixed today).
- Decisions logged: **D48** (war is a tribe-level OrgStore state; battles reuse the combat engine on adjacency; a blood-feud, not territorial; bounded so pop holds).

## Session 58 — 2026-06-25 — M16 slice 2: crime & vice (alignment-driven theft/assault/murder + rough justice)
- Did: Built the crime layer on the slice-1 combat engine. New **`CrimeSystem`** (daily): a **wicked** (`Alignment.good < crimeAlignmentThreshold`) or **desperate** (in-debt) adult may offend against a nearby folk (vicinity radius 3) — **theft** (lift `theftAmount` gold), or, if wicked **and** aggressive (brave/hot-headed/ambitious), a short **3-round brawl** (the combat engine) resolving to an **assault**, a **murder** (→ a Chronicle legend), or the aggressor's **own death** at the victim's hands. Crime **hardens** the offender (alignment drifts darker → a reinforcing D26 slide into villainy), makes them a known **outlaw** (new `Crime {thefts,assaults,murders}` rap-sheet component), turns the victim **rival**, and a **good neighbour** (`good > 0.3`) nearby delivers **rough justice** (strikes/fells the criminal). Justice is **emergent, not institutional** (no constable yet — deferred). New red **⚖ crime feed kind** + inspector **"⚖ an outlaw"** line. Recorded **D47**.
- State: **Green.** 423 tests (was 417; +6 `test/crime.test.ts`: a desperate thief robs a neighbour, a wicked bully assaults but doesn't kill a hale victim, a strong evil killer **murders** a frail one (forced — proves murder is reachable), a good/solvent agent never offends, a child never offends, and a good neighbour fells a thief on the spot). `tsc` clean. **40k soak: 0 violations**, pop stable at 60, a small **criminal underclass** (~5–7 living outlaws, ~10 thefts + a couple of assaults among them, murder rare). Determinism holds (crime draws RNG → trajectory shifts but replays per seed; properties + saveload green). **Live-verified** (preview, seed 8, ~20k ticks, no console errors): 3 living outlaws incl. a 4-theft pickpocket and a 2-assault bully; the inspector reads "⚖ an outlaw — 4 thefts". Murder didn't fire at seed 8 in 20k ticks (it's the rare dramatic exception — the unit test proves the path).
- Next: **M16 slice 3 — War** (martial tribes with low inter-tribe relations fight over territory; casualties → Chronicle legends), reusing the combat engine + the M14 Org store. That completes M16.
- Blockers/Questions: none. Deferred (ROADMAP): a formal justice system (constables/prisons/trials), a reputation graph, gangs/organized vice.
- Decisions logged: **D47** (crime is alignment-driven + self-policing — offenders harden, victims defend, a good neighbour delivers rough justice; institutional justice + gangs deferred).

## Session 57 — 2026-06-25 — M16 slice 1: combat engine + the predator threat model + wounds/veterans
- Did: Opened **Milestone 16 (Conflict)** with the combat foundation. New **`src/sim/combat.ts`** — a pure, deterministic exchange (`rollAttack`) from the combatants' **Body** (STR→damage, DEX→hit/dodge, CON→toughness) + culture **`martial`** (the long-deferred M10 axis finally gets a home) + personality **ferocity** (brave/hot-headed press, timid/gentle hold back — the deferred M13 hook) + **prowess** hardened by scars/kills; damage is in **Health (0..1) units** so a blow just lowers health, reusing the mortality machinery (no parallel HP stack — D46). New **`CombatSystem`** (after fauna act, adjacency-based): a predator beside a folk may **strike** (rare, `predatorAggressionChance` 0.012); the folk **fights back** and a strong/brave one **slays** the beast (a kill → "veteran"); a deep wound leaves a permanent **scar**; a lethal blow kills (cause "slain by a …" → a Chronicle legend + feed line). New lazy **`Combat {scars,kills}`** component, an inspector **"⚔ a veteran"** line, and feed events. Folk hunting fauna for food already existed (M8). Also fixed a **latent leak**: `killAgent` now strips Body/Alignment/Personality/Combat (M13 added them but never removed them on death). Recorded **D46**.
- State: **Green.** 417 tests (was 407; +10 `test/combat.test.ts`: hitChance/hitDamage shape + bounds, rollAttack hit/miss by rng, beastCombatant scaling, combatantOf reads Body/defaults/prowess, markCombat accumulation, and CombatSystem integration — a predator wounds a folk, a strong folk slays the beast & earns a kill, a lethal blow tombstones a near-dead folk, a grazer never attacks). `tsc` clean. **40k soak: 0 violations**, **pop stable at 60, fauna stable at 130** (predators not over-culled — FaunaSystem replenishes), kills 27→45, **scars rare (4→7) and veterans a minority (13→18 of 60)** so "veteran" stays meaningful; graves only modestly up (combat deaths occasional, not a massacre). Needed one **retune** (first pass: kills=0 because the kill-blow threshold exceeded realistic folk damage, and 50/59 folk were scarred — lowered aggression 0.05→0.012, raised scar threshold 0.22→0.3, lowered kill-blow 0.3→0.18). Determinism holds (combat draws RNG → trajectory shifts but replays per seed; properties + saveload green). **Live-verified** (preview, seed 8, ~14k ticks, no console errors): feed shows "Mipe Gra-rkza was mauled by a pallid stalker / fought off and slew a pallid stalker"; 8 living veterans incl. a 4-kill monster-slayer whose inspector reads "⚔ a veteran — 4 kills".
- Next: **M16 slice 2 — Crime & vice** (alignment-driven theft/assault/murder + a justice response), reusing the combat engine + the M13 evil→crime hook. Then slice 3 (war between tribes).
- Blockers/Questions: none. Deferred (noted in ROADMAP): weapons/armour items (await inventory), dismemberment/poison, prisons/slavery/drugs/brothels, military hierarchy.
- Decisions logged: **D46** (one ability-score-driven combat engine; damage in Health units reusing the mortality machinery, not a parallel HP system; martial/ferocity/prowess modifiers; weapons/armour a deferred hook).

## Session 56 — 2026-06-25 — M15 slice 2b: business turnover (farms fold & found) → M15 DoD met
- Did: Closed M15 with the turnover the S55 finding (D45) called for — a **fixed operating cost** is the missing ingredient that makes failure real. New **`BusinessSystem`** (daily, after the economy): each food business pays `farmOperatingCostPerDay` (10g); a farm whose balance stays below `bankruptcyThreshold` past `bankruptcyGraceDays` (14) **folds** (destroyed → its workers' jobs drop next tick → they fall back to subsistence/rehire); a **new farm opens** when food is dear (`farmSupply < demand`) AND every existing farm is full (so it'll actually find workers), capped at `maxFarms` (8). So the farm sector **tracks demand** without churn: it grows when food is scarce and culls its excess when food is cheap. Re-added `Business.lowFundsDays`, 4 config knobs, and re-homed `findPassableTile` into `world/tilemap.ts` (now genuinely shared). Legible: feed events ("a farm folded / opened — food is dear"), the 🌾 (market-funded) + ⚠ (losing money) markers in the Economy tab, and a `biz=N(farm=M)` field in the soak line.
- State: **Green.** 407 tests (was 402; +5 `test/market.test.ts`: an unprofitable farm folds past the grace under the operating cost, a well-funded one survives, a non-food business pays no overhead & never folds, founds-a-farm-when-dear-and-full, no-found-when-supply-meets-demand). `tsc` clean. **40k soak: 0 violations**, pop stable at 60, **farm count self-regulates 2→1→3→5** tracking demand, food price stabilises to ~2.5g (was climbing to 4.4g with a fixed 2 farms), and **in-debt fell 28→7** (the supply now grows to meet the mouths) — a real win for the "debt is balanced" DoD clause; Gini also eased 0.69→0.53. Determinism holds (founding consumes RNG → trajectory shifts from S55 but replays per seed; properties + saveload green).
- Next: **Milestone 16 — Conflict: Combat, Hunting, Crime & War.** Planned as three slices: (1) **combat foundation + hunting + wounds** (an ability-score-driven resolution engine, folk hunt fauna, HP/wounds persist), (2) **crime** (alignment-driven theft/assault/murder + a justice response), (3) **war** (martial tribes with low relations fight over territory, recorded as legends). Start with slice 1.
- Blockers/Questions: none. **M15 DoD met (S54–S56):** prices respond to scarcity; agents trade; property accumulates/is lost; landlords exist (M11); businesses open and fold; debt is balanced; legible; soak-stable. Crafting/skills folded into M17; banking/caravans → backlog.
- Decisions logged: none new (S56 executes the D45-prescribed fixed-cost model; turnover is the natural completion of slice 2).

## Session 55 — 2026-06-25 — M15 slice 2a: food businesses earn from real market sales (+ bankruptcy: a built-then-deferred finding)
- Did: **(shipped) Real sales revenue.** Food producers (farms) now live on **real household demand** instead of conjured revenue. `MarketSystem` (after setting the price) distributes the day's farm-food spend — `foodSalesGold = min(farmSupply, demand) × price` (households buy the farms' output at the market price, never more rations than are eaten; the foraged commons is self-gathered, no seller) — across the food businesses **by their workforce share** (`foodBusinessWorkers`); `EconomySystem` now **withholds the synthetic `revenuePerWorkerPerTick` from `producesFood` businesses** (other trades keep it until their goods get their own markets, a later slice). So a farm's balance now tracks the food economy. Pure arithmetic (no RNG) → soak byte-identical & deterministic. **Legible:** a **"Farm sales" g/day** figure in the Economy tab's Market section + **🌾 markers** on the food businesses in the table. **(built, then deferred — a recorded finding) Bankruptcy + business turnover.** I built the full thing (a `BusinessSystem`: a struggling farm folds past a grace period; a new business opens to keep the count near target, favouring a farm if food collapsed) with 6 passing tests — **then removed it**, because it is *structurally dormant*: with per-worker-share sales, the food sector's revenue (≈ demand × price, floored) tracks the mouths while wages track the workers, so the two stay proportional and **every staffed farm is always profitable**. Two honest attempts to make failure fire naturally — the default 40k soak and a **forced-glut stress run** (tiny foraging baseline + high farm output, s/d 62/44) — both produced **zero closures**. Per the two-strike rule I stopped rather than chase a fixed-cost/population-scaled-count rabbit-hole (which is what failure actually needs), and deferred it to **slice 2b** with the finding. Recorded **D45**. (Reverted cleanly: deleted `BusinessSystem`, its 3 config knobs, the `lowFundsDays` field, the `findPassableTile` move, and the bankruptcy tests — kept the sales-revenue half.)
- State: **Green.** 402 tests (was 397; +5 net `test/market.test.ts`: `foodSalesGold` scarcity-vs-glut, `foodBusinessWorkers` counts food workers only, MarketSystem credits food businesses by 2:1 worker share, a food business gets **no** synthetic revenue (balance moves by wages only), and a non-food business **still** books synthetic revenue). `tsc` clean. **40k soak: 0 violations**, pop stable at 60, **byte-identical to S54** (sales revenue moves only business balances — not a soak invariant, no RNG) → determinism intact; food still 2.1g→4.4g as the town matures. **Live-verified** (preview, seed 8, ~33 days, no console errors): the Economy tab shows "Price 2.0g/day · Supply 28 · Demand 19 · **Farm sales 20g/day**" (= min(10,19)×2.0 ✓) and 🌾 on the food businesses.
- Next: **M15 slice 2b — business turnover** (bankruptcy + founding) with the model the finding calls for: a **fixed operating cost** per business + a **population-scaled business count**. OR move on to the next M15 bullet — **resource → craft → goods** coupling + skill/knowledge gating — and fold turnover in there. Pick per the Continue Loop.
- Blockers/Questions: none. Note: "businesses can go bankrupt" (an M15 DoD clause) is **not yet live** — the mechanic is understood and specced (D45) but needs the fixed-cost model to actually fire; it's slice 2b.
- Decisions logged: **D45** (food businesses earn from real market sales; bankruptcy is structurally dormant without fixed costs + pop-scaled counts, so deferred to slice 2b with the finding).

## Session 54 — 2026-06-25 — M15 slice 1: a staple-goods market — prices move with supply & demand
- Did: Opened **Milestone 15 (Economy Depth)** with its first slice — the **supply/demand price for the town staple**. New **`Market`** singleton (`src/sim/market.ts`, a store like cultures/orgs) + a daily **`MarketSystem`** (`src/sim/systems/MarketSystem.ts`, ordered before `EconomySystem`): each day it reads **supply** = (food-workers' output `provisionPerFarmer` + a wild-foraged baseline `baseForagedProvisions`, the commons) against **demand** = adult mouths (`provisionPerAdult`; children don't transact, per the Kids Pass), and eases the price toward the clearing target (`basePrice × demand/supply`, clamped to `[provisionPriceMin, provisionPriceMax]`, smoothed by `priceAdjustRate`). The keystone change: **the cost of living IS that price** — `EconomySystem` now charges `market.price` instead of the flat `dailyUpkeep` (fallback kept for marketless test worlds). Food producers are **content-flagged** (`producesFood: true` on `farmer.yaml` → schema → baked into `Business`; D9, not hardcoded). **Legible:** a new **Market section in the Economy tab** (price · supply · demand · a price sparkline + a one-line "dear/steady/cheap" read) and a `food=Xg(s/d S/D)` field on the soak line. **Scope split (deliberate, D44):** this slice is only the *price* mechanic — pure arithmetic (no RNG → replays per seed), bounded (the price band keeps debt ≤ maxDebt). **Real sales revenue** (routing household spend to producers, replacing the synthetic per-worker revenue) + **business bankruptcy/open-close** are **slice 2** — they need a wage-loop rebalance (the wage/gather loop dwarfs food retail, so a naive swap destabilises) and carry cascade risk. Recorded **D44**.
- State: **Green.** 397 tests (was 387; +10 `test/market.test.ts`: clearingPrice shape (equilibrium=base, scarcity↑/glut↓, stays in band under extremes), measureSupplyDemand (adult demand excl. children, farmer supply + baseline, non-food biz adds demand not supply), MarketSystem (daily update + bounded history, no-op off a day boundary), the **causal DoD test** (a dear-food adult loses more to upkeep than a cheap-food one), the marketless fallback, and world-gen wiring). `tsc` clean. **40k soak: 0 violations**, pop stable at 60 — and the market tells an emergent story: a **young foraging town has cheap food (2.1g, supply>demand)**, then as it fills the cap and the **adult share outgrows the farms**, demand passes supply and **food gets dear (→4.4g)** with **debt rising in step (0→28, bounded ≤ maxDebt)**. Determinism holds (price→gold→action timing shifts the trajectory but the same-seed replay test passes). **Live-verified** (preview, seed 8, ~25 days, no console errors): the Market is live in the app (price 2.05g, s/d 28/19); the Economy tab's Market section renders price/supply/demand + a 25-point sparkline.
- Next: **M15 slice 2 — real sales revenue + bankruptcy** (route household food spend to food producers as revenue, replacing the synthetic per-worker revenue; let a broke business fail and a new one open) — the part that needs the wage-loop rebalance. Then resource→craft→goods + skill gating, banking/loans/debt rebalance, and the per-agent holdings view.
- Blockers/Questions: none. Note: mature-town debt now climbs with food prices (0→28 of 60 by yr 38, all bounded ≤ maxDebt) — the intended "scarcity bites" effect; the **banking/loans/debt rebalance** (a later M15 bullet) is where that gets softened if wanted.
- Decisions logged: **D44** (a staple-goods market: one price floats with supply/demand and IS the cost of living; price-only this slice; sales-revenue + bankruptcy deferred to slice 2; completes part of M3's deferred market).

## Session 53 — 2026-06-25 — M14 Institutions: tribes that form, govern & schism (DoD met)
- Did: Built the unified **Organization** engine (D33→D43). Tribes/factions are a culture-style **store** (`src/org/orgStore.ts`, `byId` of `Organization` records agents reference by `orgId`): each has a language-coined name, a **hue-spaced colour (never red)**, values → an **emergent government** (chiefdom/council/theocracy/gerontocracy/meritocracy), a `leader`, cohesion, and descent — reusing the culture/language **fork machinery** (`createOrg`/`forkOrg`/`pruneOrgs`). **World-gen** splits the founders into `initialTribes` (3) tribes (households kept together; values seeded from their culture); **children inherit the mother's tribe** (`spawnAgent`/`ReproductionSystem`). New **`OrgSystem`** (daily): **leadership succession** (eldest takes over when a head dies) + **extinction** (memberless tribe falls); on the **era cadence**, **schism** — a large, loose tribe sheds a breakaway faction with a new colour/name/nudged-values. Legible: folk are **tinted by tribe colour** (`renderer`), a **Tribes dashboard** (hotkey **K**, `orgDashboard.ts`), and an inspector **Allegiance** line (tribe · government · "leads it"). One config knob (`initialTribes`). **Deferred:** social class + reputation graph (folded into M15) and tribe fusion. Recorded **D43**. **Bonus fix:** the M14 RNG-shift surfaced a latent collision — newborns spawned *on* the mother's tile (a one-tick shared-tile break caught when a birth lands on a sample tick); they now arrive on a **free adjacent tile** (`ReproductionSystem`).
- State: **Green.** 387 tests (was 379; +8 `test/org.test.ts`: hues-never-red, government-from-values, createOrg/forkOrg/pruneOrgs, OrgSystem succession/extinction/schism, and seeded founding tribes). `tsc` clean. **40k soak: 0 violations** (after the newborn-tile fix; was 1 transient pre-fix), pop stable at 60; tribes form (3) and **schism to ~9 over the run, bounded** by the lineage cap. Determinism holds (more RNG at spawn/era, replays per seed). **Live-verified** (seed 8, ~26k ticks, no console errors): **7 living tribes (4 via schism)** from 3 founding, all 60 folk assigned; distinct non-red colours, emergent governments (gerontocracy/chiefdom), named leaders; inspector reads "Tribe Rkkharur clan / gerontocracy"; the Tribes tab renders; folk carry their tribe's tint on the map.
- Next: **Milestone 15 — Economy Depth: Markets, Trade & Class** (supply/demand pricing, trade, and **social class** — which gives the deferred M14 class/reputation thread its meaning).
- Blockers/Questions: none. The one open M14 thread is **social class + reputation**, deliberately folded into M15 (the market is what makes class mean something).
- Decisions logged: **D43** (institutions as a culture-style store; tribes form at world-gen + schism over the eras; class/reputation deferred to M15).

## Session 52 — 2026-06-25 — M13 Agent Depth: heredity, ability scores, dynamic alignment, personality (DoD met)
- Did: Gave agents real D&D-style depth via **one Heredity system** (`src/sim/heredity.ts`). New components: **`Body`** (STR/DEX/CON/INT/WIS/CHA rolled 3d6 + content-driven per-species `abilityMods` — dwarves +CON/+STR/−DEX/−CHA via an optional schema field — plus height/build/eye/hair as 0..1 shades), **`Alignment`** (good/law axes → the classic 3×3 grid), **`Personality`** (one trait from a weighted palette). Founders roll; **children inherit the parental mean + variation** (and usually take after a parent for traits/lean) — so stats and looks visibly run in families. **Causal couplings (D26), each small/centred/bounded:** CHA warms friendship (`charismaWarmth`), good alignment makes folk **cooperate** (`alignmentWarmth`), ambitious/greedy vs content/generous **bend the wealth goal** (`traitGoalFactor` in ActionSystem); **alignment drifts with the life lived** (bonds→good, loss→harder) and **deep loss hardens** personality, both at reflection (reusing the M10 distill, no RNG). STR/DEX/CON/INT/WIS + evil→crime + brave/timid are **foundational** with homes in M16 (combat), like `martial` was. Inspector gained a **"Body & character" agent sheet** (scores, physical line, personality · alignment). **Inventory deferred** (status states already exist as needs; items need uses from M15/M16). Recorded **D42**.
- State: **Green.** 379 tests (was 364; +15 `test/heredity.test.ts`: rollBody range + species mods, inheritBody resemblance incl. the **light-eyed→light-eyed DoD test**, charismaWarmth, alignment roll/inherit/name/warmth + the **shifts-with-experience drift**, personality palette/inheritance/goal-factor + **ambition→behaviour** + **trauma→hardened**). `tsc` clean. **40k soak: 0 violations**, pop stable at 60 (spawn now rolls bodies/alignment/personality → the RNG trajectory shifts but replays per seed). **Live-verified** (seed 8, no console errors): agent sheet reads "STR 10 DEX 7 CON 7 / INT 11 WIS 5 CHA 13 / 136cm · average build · dark brown eyes · blonde hair / gregarious · True Neutral"; town shows 9 distinct personalities + a 3-way alignment spread (mean good +0.26 — benevolent-leaning); **76% of children share a trait with a parent**; a child's eye/height track the parental mean.
- Next: **Milestone 14 — Institutions** (the unified `Organization` engine: tribes/factions/governments, membership, leadership + succession, procedural colour, territory, reputation, schism reusing the culture/language fork machinery).
- Blockers/Questions: none. The one open M13 bullet is **inventory** — intentionally deferred (a carried inventory is hollow until crafting/trade/combat give items uses); the DoD is otherwise met.
- Decisions logged: **D42** (one Heredity system; causal-now vs foundational-later couplings; status reuses needs; inventory deferred).

## Session 51 — 2026-06-25 — Stress test + a debt-cap bug fix it surfaced
- Did: A dedicated stress pass (well past the standard 40k/64×64 soak) — **deep time** (150k ticks ≈ 156 sim-years, 64×64), a **big map at scale** (200×200, pop → 207 of a 586 cap), **8-seed robustness** (40k each), and a **large-world snapshot round-trip** (2 MB). It surfaced one real bug: **debt could reach 41, one past the `maxDebt` cap of 40** — `EconomySystem` clamps debt to the cap, but `RentSystem` runs *after* and its rent `spend` ticked a maxed-out tenant from 40→41 with no re-clamp. **Fixed** by clamping the tenant's debt to `maxDebt` after rent in `RentSystem` (+1 regression test in `rent.test.ts`).
- State: **Green.** 364 tests (was 363; +1 rent debt-cap regression). `tsc` clean. **Stress results (all pass after the fix):** deep time — pop steady at 60, fauna ~130, homes bounded 33–46, memory bounded (maxMem ~35, total ~1500, no leak), eras capped 8, samples capped 80, tongues 4→15 (< the maxLineages 24 cap), 0 violations across 156 years. Big map — 200×200 at **4.2 ms/tick (~240 ticks/s)**, worldgen 29ms, 0 violations, pop/fauna/homes scale sanely. 8 seeds — all stable, 0 violations, maxDebt ≤ 40. Snapshot — 2.09 MB world serialises in 20ms, **loads in 75ms**, byte-identical AND continuable.
- Next: **Milestone 13 — Agent Depth** (ability scores, dynamic alignment, personality, one Heredity system, inventory/status, a rich agent sheet).
- Blockers/Questions: none. The sim is robust under deep time, scale, many seeds, and large-world save/load.
- Decisions logged: none new (the debt-cap fix enforces D39's existing cap consistently across systems).

## Session 50 — 2026-06-25 — M11 COMPLETE (rent + civic) + M12 COMPLETE (snapshot save/load, named worlds, disk) — big push
- Did: A large, multi-feature session (user asked to complete both milestones). **M11 finished:** **(rent)** new `RentSystem` (daily) + `Agent.rentsFrom` + `rentPerDay` (1g) — a homeless adult shelters in a landlord's *spare* home (homes owned beyond the one they live in) for a daily rent, so "landlord" is a real income and a rented roof spares the homeless mood penalty (deterministic matching, bounded by spare capacity; inspector shows "renting from X" / "owns N — landlord (T tenants)"). **(civic)** new `Civic` component + landmarks (Town Hall / Well / Shrine) placed at world-gen **deterministically after RNG gen** (trajectory unchanged), rendered as a bannered hall, click-inspectable, in the legend — the map now reads Workplace / Home / Civic (hooks for M14/M15). **M12 finished:** **(snapshot)** saves now carry a full **world snapshot** (every ECS component + RNG state) so `loadSave` rebuilds state **instantly** instead of replaying — `World.snapshot()/fromSnapshot()`, RNG `getState/setState`, a Uint16Array-safe JSON (de)serializer for the TileMap; **replay kept as the fallback/baseline**. **(named worlds)** new `saveStore.ts` (IndexedDB) + a **Saved worlds** pause-menu screen: name + Save current, a list (Year/pop/seed) with per-row **Load** (instant) and **Delete**. **(disk)** Export current → a portable `.omnia` file; Import → file picker → load. Old single-slot localStorage save retired. Recorded **D42**.
- State: **Green.** 363 tests (was 355; +5 rent, +1 civic, +2 saveload snapshot; mood/homes harnesses reused). `tsc` clean. **40k soak: 0 violations**, pop stable; rent adds realistic bounded inequality (Gini 0.59, adults 0.55; debt bounded ≤ maxDebt), mean mood 0.74, homes ~38 bounded. **Determinism:** rent shifts the trajectory (gold→work/wander timing) but replays per seed; civic placement + all M12 work touch no RNG. **Live-verified** (seed 8, no console errors): 3 civic landmarks (Town Hall, inspectable); 4 landlords / 4 tenants ("renting from Bdrudradak Ba-zo"); and the **full M12 round-trip** — saved "Verify World" (~0.6 MB) to IndexedDB, listed it, **instant-loaded** it (tick 14000, pop 57, 17 homes restored from snapshot), and deleted it.
- Next: **Milestone 13 — Agent Depth** (ability scores, dynamic 9-alignment, personality, one Heredity system, inventory, status; a rich agent sheet). First slice likely the Heredity system (generalising heritable magic aptitude) or ability scores. Inventory there also unlocks the deferred "build homes from gathered timber" (M11) and meaningful uses for gold.
- Blockers/Questions: none. Notes: rent makes renters the poorest (bounded, capped debt) + landlords a bit richer — intended/realistic; **eviction**-on-debt (deferred) would complete debt's consequence. Civic buildings are flavour hooks (no mechanical role until M14/M15). Export/import verified by code + the shared serialize/load path (the IndexedDB round-trip exercised the same bytes); a manual file download wasn't auto-triggered.
- Decisions logged: **D42** (snapshot save/load + IndexedDB named worlds + disk; replay retained as baseline; extends D5/D19). Rent/civic execute the M11 plan (rent extends D39/D40).

## Session 49 — 2026-06-25 — M11 slice 2: the first mood system + sleep-at-home (homes that matter)
- Did: Gave homes a real benefit — **mood**, the first well-being system (the README's "happy and sad"). New **`Agent.mood`** (0..1, optional like `fluency`/`purpose` → no `Needs`-literal churn; seeded at `MOOD_BASELINE` 0.6) + daily **`MoodSystem`** (`src/sim/systems/MoodSystem.ts`, after HealthSystem): a target is set from circumstance — owning a **home** (+0.2), living **family** (partner/parent/child, +0.15), **debt** (−0.2), **homelessness** (−0.1, adults only — children are dependents), **illness** (−0.15) — and mood drifts ⅓ of the way toward it each day. Mood is **causal** (D26): `SocialSystem.interact` scales friendship warmth by `moodWarmth(a,b)` (centred on baseline, so typical folk unchanged), so contentment spreads connection — and **debt finally has a comfort consequence** (the bite deferred in D39). Folk now **head home to sleep** (`MovementSystem` routes a sleeper to their own bed) and one's own bed rests faster (`HOME_REST_BONUS` 1.4). Legible: inspector Mood bar (content/unsettled/low); `townMood` in `measureWorld` + a soak "wellbeing" line. Weights are hardcoded constants (like `bondFactor`/`distill`). Recorded **D41**.
- State: **Green.** 355 tests (was 346; +9 `test/mood.test.ts`: target up/down by circumstance, children not homeless-penalised, bounded over 60 days, day-boundary gate; `moodWarmth` shape; a content pair warms faster than a low-mood pair; sleeper steps toward home; home rests faster than rough sleeping). `tsc` clean. **40k soak: 0 violations**, pop stable; **mean mood 0.76** (a content town — homes + families lift it), homes bounded 14→41, Gini 0.65→0.51. Determinism holds (MoodSystem + mood→social consume no RNG; the only new RNG draw is the sleep-at-home *movement*, so the trajectory legitimately shifts from S48 but replays per seed). **Live-verified** (seed 8, no console errors): **homeowners avg mood 0.90 vs homeless 0.60**; inspector shows "Mood █████████░ 95% content".
- Next: **M11 slice 2 remainder — landlord rent** (the homeless shelter with landlords for a fee → "landlord" becomes mechanical income; eviction-on-ruinous-debt would complete debt's consequence). Then slice 3 (buildings fully generalised: home/workplace/civic).
- Blockers/Questions: none. Note: mood deliberately does **not** yet bias action selection or mortality (kept to the social coupling for soak-safety); deeper coupling is a future option if wanted.
- Decisions logged: **D41** (first mood/well-being system; circumstance→mood→friendship; homes & solvency lift it; executes D26/D35, completes part of D39).

## Session 48 — 2026-06-25 — Two fixes (child beliefs, clickable homes) + M11 slice 2 starter: home inheritance
- Did: Two reported fixes plus the first slice-2 feature. **(1) "finds meaning in their children" on children/childless folk** — root cause: a **newborn** gets a `was born to X` memory (`ReproductionSystem`), and the stub's `family` theme keyed on the word **`born`**, so a baby reflected as if it had children. Fix: dropped `born` from the family keywords (it also matches one's *own* birth) — the theme now fires only from genuine parenthood memories (`their child …`, `lost their child`) — and **reworded** the family lines per the request (`finds meaning in their children`→`…their family`; `give their children a better life`→`…their kin…`). Net: only actual (current/bereaved) parents ever get a family belief; children and childless adults never do. **(2) Homes weren't clickable** — the home inspector shipped in S47 but `renderer.consumeClick` never checked `C_HOME`, so a click never selected a house. Added `C_HOME` to the pick chain (folk > fauna > business > **home** > resource > flora). **(3) Slice-2 starter — home inheritance:** a home outliving its owner now passes to a **living, home-less child** (the family seat, kept down the line) and only ruins when there's no such heir — so homes run in families/dynasties while staying bounded (the escalating build cost caps each agent's total). Recorded under D40 (which had already flagged inheritance as the slice-2 plan).
- State: **Green.** 346 tests (was 343; +1 `ai.test.ts` self-birth-isn't-family + the reworded assertion, +2 `homes.test.ts` inheritance/heir-already-owns; updated `soul2.test.ts` to the new family wording). `tsc` clean. **40k soak: 0 violations**, pop stable; homes **bounded at 39** for pop 60 (vs 35 without inheritance — inheritance preserves more but the escalating-cost cap holds it), Gini still falls 0.70→0.52. **No RNG touched** (the stub reword is flavour; inheritance is reassign + `remember`, no rolls) → the demographic trajectory is unchanged from S47. **Live-verified** (seed 8, no console errors): clicking home tile (17,16) selects it → inspector "a dwelling · owned / Owner Dubka Rkaz-dba / Built in year 0"; a scan of the living shows **0 children and 0 childless adults with a family belief** (only parents have them).
- Next: **M11 slice 2 remainder** — sleep-at-home + a **mood/comfort** stat (the first mood system; also unlocks debt's deferred eviction consequence from D39), then **landlord rent**. Then slice 3 (buildings fully generalised: home/workplace/civic).
- Blockers/Questions: none.
- Decisions logged: none new (the belief fix is a correctness fix; inheritance executes the slice-2 plan already recorded in D40).

## Session 47 — 2026-06-25 — M11 slice 1: folk build & own homes (the town visibly grows)
- Did: Started Milestone 11 with its foundational slice — **homes you build and own**. New `Home` component (`{owner, builtTick}` + Position; static, no brain) and **`BuildSystem`** (runs daily, after the economy): a settled **adult** with `homeCost` (40g) in hand **builds a home** where they stand — placed by a deterministic outward ring-scan onto a passable, building-free tile — so the town **visibly grows** over the generations. Cost is **gold** (an abstraction of the gathering economy's output; no inventory yet — that's M13) which doubles as a **wealth sink** (gives gold a purpose, pulls the Gini down). Cost **escalates** with each home owned (`homeCost × (owned+1)`), so most own one and the wealthy become **landlords** (own several) — no special rule. A home whose owner is no longer living **falls to ruin** (pruned each pass → the count stays bounded; inheritance deferred to slice 2). Wired into the loop after `EconomySystem`. **Legible (D35):** homes render as a warm-coloured house; the legend now splits **Workplace** vs **Home**; the inspector shows an agent's home (or "owns N — a landlord") and a home's owner (living or "(late)") + build year. Children never build (Kids Pass holds). Recorded **D40**.
- State: **Green.** 343 tests (was 335; +8 `test/homes.test.ts`: affordable→builds (gold spent, owned, placed), too-poor→doesn't, children→don't, only-on-day-boundary, escalating-cost caps a second home, wealthy→landlord over two days, ruined-on-owner-gone pruned, deterministic placement). `tsc` clean. **40k soak: 0 violations** (incl. a new "homes on passable land" invariant), pop stable at 60; **homes grow 14→35 and stay bounded** (pruning + finite adults × escalating cost); the home wealth-sink pulls **Gini 0.70→0.50** over the run; debt stays low (0–10). Determinism holds (placement is RNG-free; the gold sink shifts work/wander timing, so the trajectory differs but replays per seed). **Live-verified** (seed 8, ~9k ticks, no console errors): 17 homes / 15 owners / a landlord present; an owner inspects as "Home at (17,16)"; a home inspects as "a dwelling · owned / Owner Dubka Rkaz-dba / Built in year 0".
- Next: **M11 slice 2 — homes that matter:** folk go home to sleep + a **mood/comfort** stat lifted by a home (the first mood system, which also gives debt its deferred eviction/comfort consequence from D39); **landlords collect rent** (making the label mechanical); **home inheritance** to a living heir (family homes/dynasties) instead of always ruining. Then slice 3 (buildings fully generalised: home/workplace/civic).
- Blockers/Questions: none. Note: build cost is gold (an abstraction of gathered resources) by deliberate choice (no inventory until ~M13) — recorded in D40; literal timber-building is a slice-2/M13 follow-up.
- Decisions logged: **D40** (homes are owned static entities built from savings; gold cost; ruined-on-death, inheritance deferred; extends D39).

## Session 46 — 2026-06-25 — Economy Rebalance: subsistence floor, bounded-but-biting debt, more jobs, honest Gini
- Did: Finished the economy fixes the S45 play-review surfaced. **(1) Subsistence floor** (`EconomySystem`): a jobless adult earns `subsistencePerDay` (2.5, just under upkeep) from foraging/odd-jobs, so unemployment is *bounded poverty* not a death spiral; the employed get no handout (work still pays). Children remain fully exempt (Kids Pass holds). **(2) Bounded, meaningful debt:** capped at `maxDebt` (40) in the daily loop so it can't run away; given teeth — the indebted recover from illness `debtRecoveryPenalty` (40%) slower (`HealthSystem` now reads the wallet, no RNG); escape stays via subsistence + `earn` paying debt before gold. `.debt` is no longer inert. **(3) More employers:** `businessCount` 8→12 so most working-age adults find real work (subsistence catches the rest). **(4) Honest Gini:** new `adultWealthGini` (gold among working-age adults) in `measureWorld` + the soak wealth line + the Economy tab, shown beside the all-folk Gini with an explanation that children (dependents at 0g) inflate the "all" number; added a `Jobless` metric + per-line `debt=` to the soak. Recorded **D39**.
- State: **Green.** 335 tests (was 329; +6: economy subsistence/cap/child-exempt + the debt-slows-healing health test + 2 `adultWealthGini` tests; updated the old upkeep test + a Kids-Pass test to set `subsistencePerDay:0` where they isolate upkeep). `tsc` clean. **40k soak: 0 violations**, pop stable at cap; determinism holds (no new RNG — subsistence/cap are arithmetic, the health penalty is a pure wallet read; the changed economy shifts work/wander + illness-duration timing, so the trajectory differs but replays per seed). **Measured (5 seeds, 30k):** adult debt **33–47%→14–31%** (the in-debt are now *exactly* the jobless), debt **bounded ≤40** (was unbounded, −150+), adult Gini **0.28–0.50** vs all-folk ~0.65; seed-8 soak Gini **0.87(climbing)→0.51 (adults 0.38)**, in-debt **47→~10**. **Live-verified** the Economy tab (seed 8, no console errors): "Gini · all 0.74 / Gini · adults 0.17 / In debt 0 / Adults 17 / Employed 17 / Jobless 0" with the explanatory note — the honest metric reads correctly (equal working economy; the "all" figure is high only because of the dependent children).
- Next: **Milestone 11 — Homes & Property** (agents build/own homes from gathered resources; the town visibly grows; sleep there for a mood bonus; landlords; generalise "building" to home/workplace/civic). A mood system + homes would also unlock the deferred debt consequences (eviction risk) and is a natural place to revisit the boom-bust age structure.
- Blockers/Questions: none. Deferred (in ROADMAP/D39): eviction/mood debt-consequences (await homes + mood), dynamic job-scaling with population (a fixed bump sufficed), and the demographic boom-bust (still a watch item — the subsistence floor makes the low-workforce peaks survivable).
- Decisions logged: **D39** (subsistence floor + bounded/biting debt + more jobs + honest Gini; extends D15/D17, follows D38).

## Session 45 — 2026-06-25 — The Kids Pass: children become dependents with their own minds (+ play-review diagnosis)
- Did: A directed **play-review + fix**. First ran 5 seeds × 30k ticks headless and confirmed every issue the reviewer flagged, with root causes: **(1) children in debt** — `EconomySystem` charged the daily cost of living to *everyone*, but children can't work and spawn at 0 gold → 100% of children in debt from birth; **(2) everyone in debt** — because `earn` pays debt before gold, each adult entered adulthood carrying ~190g of *childhood* debt, which + structural unemployment (~30 job slots for ~45 adults) drove 75–80% of the town into debt; **(3) Gini always climbing** (→0.87) — downstream of the gold-0 pile-up (children + debtors + jobless), and it's measured over gold only; **(4) child vows** — `distill` keys on `befriend`, so any child who makes a friend got the adult vow "to provide for those they love", with no age gate on reflection. Also found: **debt is inert** (nothing reads `.debt`) yet permanent, and the **age structure boom-busts** (kids 13%→75%→15%). Then built the **Kids Pass** (scope confirmed with the reviewer: gate economics off kids, keep illness/mortality, give kids their own graduating minds): **`EconomySystem`** skips upkeep for under-`adultAgeYears` folk; **`distill(events, isChild)`** gains a parallel **child-vow** vocabulary (`memory.ts`; make a true friend / be brave / stay close to family / learn all they can / see what the world holds) + an exported `CHILD_VOW_SET`; **`AISystem.reflectPass`** picks the vocab by age and, when a child vow gives way to an adult one, fires a **"comes of age, resolving …"** feed line + a "came of age" milestone memory; **inspector** shows children a "dependent" livelihood (no job/goal/debt). Recorded **D38**; roadmap gains the Kids Pass (done) + an **Economy Rebalance** mini-milestone (next).
- State: **Green.** 329 tests (was 323; +6 `test/childhood.test.ts`: child vs adult vow vocabulary + drive-unchanged; children pay no upkeep / never accrue debt over 50 days; and a coming-of-age integration through `runAISystem` — child vow → adult vow + feed line + milestone memory). `tsc` clean. **40k soak: 0 violations**, pop stable at cap; determinism holds (no new RNG — the economic change shifts adult work/wander *timing*, so the trajectory legitimately differs but replays per seed). **Re-measured (5 seeds, 30k): children-in-debt 100%→0%, total-in-debt 75–80%→0–45% (seed 5 hit 0%), Gini 0.87(climbing)→0.65–0.68(stable), adult mean gold ~20–28→~31–57, median net worth −100..−160→0, child vows 100% "to make a true friend".** **Live-verified** (seed 8, no console errors): a child inspects as "A child — a dependent (no work or upkeep yet)" with "⚑ vows to make a true friend".
- Next: **Economy Rebalance** mini-milestone — the residual adult debt is structural unemployment (too few jobs for the adults) + inert/permanent debt; also make Gini honest (net-worth / adults-only / shown in the Economy tab). Pick a slice per the Continue Loop (likely: scale job openings with the adult population and/or a subsistence fallback, so a jobless adult isn't doomed to unbounded debt).
- Blockers/Questions: none. Note: residual adult debt (0–45%) is expected and is the Economy Rebalance scope; the Kids Pass deliberately stayed one clean slice (reviewer chose kids-first). Boom-bust age structure is logged as a "watch" item.
- Decisions logged: **D38** (children are economic dependents with age-appropriate minds that graduate at coming-of-age; refines D17, executes D26).

## Session 44 — 2026-06-25 — M10 slice 4.5 + COMPLETE: Conversation/Language tabs, visible chat, bilingualism metric
- Did: The legibility half of slice 4 — two new master tabs, the visible-chat badge, and a town-level language metric. **Conversation tab** (`src/render/conversationDashboard.ts`, hotkey **V**): a pure read of the EventLog, split into **Said aloud** (dialogue) and **Dreams, resolutions & beliefs** (dream/decide/reflect), newest first — the town's voices, separated from the births/deaths in the Town Happenings ticker. **Language tab** (`src/render/languageDashboard.ts`, hotkey **N**): per living tongue, its **phoneme inventory** (consonants/vowels), **syllable shapes**, **native vs learned** speaker counts, the cultures that speak it, and **coined sample names** (so you can hear the drift between a parent tongue and its daughters), all under a **bilingualism summary** (living tongues · % bilingual · tongues/head · lingua franca). **Visible chat** (`renderer.ts`): a render-only "in company" pass — two pink speech-dots drawn when a folk stands beside another folk, **decoupled from the rare `socialize` action** (which only fires on a low social need), so adjacency-driven talk reads on the map. **Bilingualism metric** `linguisticDiversity` (`analysis/metrics.ts`): speakers, distinct tongues commanded (≥0.5), bilingual fraction, mean tongues/head, and the **lingua franca** (the tongue of widest total command) — wired into `measureWorld` + the soak science block. Also fixed the in-app **Controls keymap** (`menu.ts` was missing Ecology·Y since S42; added Y/V/N) and the README controls table.
- State: **Green.** 323 tests (was 320; +3 `metrics.test.ts` for `linguisticDiversity`: monolingual town, bilingual-counting above the fluency bar + lingua-franca pick, empty-safe; + integration assertions in the live-loop measureWorld test). `tsc` clean. **40k soak: 0 violations**, and the **demographic trajectory is byte-identical** to S43 (the additions are read-only metric + render/tabs — no sim RNG touched). New soak line: `fluency: speak=9 tongues bilingual=97% mean=5.02/head lingua franca=old_vant.d4800 (90% command)` — over 42 sim-years a small dense town becomes highly multilingual and a daughter of Old Vant emerges as the common tongue. **Live-verified** (preview, seed 8, ~6000 ticks, no console errors): the Conversation tab renders real dialogue ("Ulok Neok to Osker Lemel: 'A fine, quiet day.'") + inner life; the Language tab shows Old Vant at **19 native + 17 learned** (lingua franca forming), the lone Drakhan native + 4 learners, drifted daughter phoneme sets, and audibly different sample names (Minu/Mirel vs Atsana/Unpi); 14% bilingual mid-run.
- **M10 DoD met (S38–S44):** culture (all axes), beliefs, and language each measurably steer behaviour (a test per loop); the master tabbed view holds every global view (Legends/Economy/Find/Family/Lineages/Ecology/Conversation/Language); Mind stays in the entity inspector (its per-agent home); the sleep/work jitter is gone (S39); determinism + soak hold throughout.
- Next: **Milestone 11 — Homes & Property**: agents **build/own** homes from gathered resources over time (the town visibly grows), sleep there for a mood bonus, store goods; landlords own several; generalise "building" to home/workplace/civic. First slice likely the **build-a-home** loop (a new `Home` component + a build action consuming resources) or **generalising buildings** — pick one per the Continue Loop.
- Blockers/Questions: none. Two observations on the language model (both documented as future refinements, neither a bug): (1) learning is **universal** (not yet gated by the `open` axis); (2) closely-related daughter tongues are treated as **distinct ids** (no partial intelligibility from shared phonemes), so a small dense town's bilingualism count runs high (5 tongues/head) — believable here, but the lever is slower learning / partial cross-tongue intelligibility if more distinct linguistic communities are wanted.
- Decisions logged: none new (legibility execution under D35; the metric extends the D29 instrumentation; the chat badge keeps sim/render separation — it's computed render-side from positions, no sim state added).

## Session 43 — 2026-06-25 — M10 slice 4: language as a causal mechanic (fluency, synergy, gradual learning)
- Did: Made **language a real mechanic**, not just flavour on names — the causal half of slice 4 (the legibility half, the Conversation/Language tabs + visible chat, split out as slice 4.5; mirrors the slice 3 → 3.5 mechanic/legibility split). New per-agent **`Agent.fluency`** map (`languageId → 0..1`; optional, like `cultureId`/`surname`/`purpose`, so the ~20 test Agent literals didn't churn): natively fluent (1) in one's culture tongue, others **learned** through contact. New **`src/lang/fluency.ts`** (pure, no RNG/LLM): `nativeFluency`, `intelligibility(a,b)` (max over shared tongues of the *weaker* speaker's fluency; neutral 1 when a map is absent, preserving old behaviour in cultureless unit tests), `langSynergy(intel, floor)`, `learnTongue` (bounded growth toward 1). **`spawnAgent`** sets native fluency from `culture.language`. **`SocialSystem.interact`** now warms friendship by **`bondFactor × langSynergy(intelligibility)`** — `open` (willingness, slice 2) and intelligibility (ability) BOTH gate cross-group bonding; a shared tongue bonds at full rate, no common tongue at `langSynergyFloor` (0.4) — and each speaker **gradually learns** the other's native tongue per interaction (`langLearnPerInteract` 0.002). Two new config knobs (added to `config.ts` + mirrored to `config/simulation.yaml` for the drift guard). Inspector shows an "Also speaks X 92%" line (legibility, D35). Saveload untouched (replay-based → fluency reconstructs on replay).
- State: **Green.** 320 tests (was 314; +6 `test/fluency.test.ts`: the four pure helpers incl. partial-overlap intelligibility & bounded learning; an integration where a shared-tongue pair warms faster than a no-common-tongue pair with culture held neutral (the DoD "language shifts an outcome distribution" test); and one where two different-tongue neighbours each learn the other's tongue over 300 ticks → mutually intelligible). `tsc` clean. **40k soak: 0 violations**, pop steady at cap (60), and the **demographic trajectory is byte-identical** to the prior reference (yr=38 folk=60 born=58 graves=22 mages=3 cultures=8 tongues=8 drifts=15) — the mechanic consumes **no sim RNG**, so determinism held exactly (only sentiment-derived social-graph metrics can shift, and only for cross-tongue pairs). **Live-verified** (preview, seed 8, ~4000 ticks, no console errors): 6 of 30 agents became bilingual through contact; the lone **Drakhan** dwarf (Zorkkob) reached **100% Old Vant** (a minority speaker assimilating to the majority tongue), several Vant natives picked up Drakhan 41–92%; the inspector renders "Tongue Drakhan" + "Also speaks Old Vant 100%". (Screenshot tool timed out as before — the rAF loop never idles for a capture; DOM evals are instant + correct, so it's the known capture artifact, not an app bug.)
- Next: **M10 slice 4.5 — Conversation/Language master tabs + visible chat** (Conversation tab = active/past dialogue; Language tab = phoneme inventory, sample words, sound-change history, fluency/bilingualism spread; decouple the "chatting" badge from the rarely-low social need; + a bilingualism metric in `analysis/metrics.ts`). After 4.5, M10's DoD is met (culture/beliefs/language all causal ✓; the remaining clause is the tabs).
- Blockers/Questions: none. Note: learning is currently **universal** (any contact teaches), not yet gated by the `open` axis — a documented future refinement (insular folk slower to learn an outsider's tongue) in `CULTURE_AND_LANGUAGE.md`. Only friendship *warmth* is openness-gated today.
- Decisions logged: none new — executes **D26** (a small, soak-gated, deterministic causal coupling; the LLM stays off-trajectory) exactly as slices 2–3 did; the mechanic/legibility split follows the slice 3 → 3.5 precedent.

## Session 42 — 2026-06-23 — M10 slice 3.5: emergent-structure descriptions + 4 new metrics + Ecology tab + feed fix
- Did: A requested legibility pass on top of slice 3. **(1) Plain-language descriptions** — every article in the **Emergent structure** panel (`legendsPanel.scienceHtml`) now carries a one/two-sentence "what it is + why it matters" line, reworded to drop jargon (the ask was descriptions anyone could understand). **(2) Four new pure metrics** (`analysis/metrics.ts`, same no-RNG discipline as the rest): **`vowSpread`** (distribution of the slice-3 vows + mean `purpose` → "town leans hopeful/weary"), **`dynastyConcentration`** (share of the living in the biggest paternal surname-line + a Gini over line sizes — emergent dynasties), **`matingAssortativity`** (observed within-culture pairing vs a random-pool baseline → a normalized homophily index, melting-pot↔segregation; showcases slice-2's endogamy), **`occupationDiversity`** (Shannon evenness over professions → division of labour). All four added to `measureWorld` + rendered. **(3) New `EcologyDashboard`** (`render/ecologyDashboard.ts`, hotkey **Y**, registered as the 6th master tab): the wildlife had no view, yet the M8 predator–prey web is one of the liveliest systems — shows grazers vs predators + the predator:prey ratio, per-species head-counts with a "condition" (avg-hunger) bar, and flora with its forageable-ripeness share. Pure read of `C_FAUNA`/`C_FLORA`. **(4) Town Happenings feed fix** (`eventFeed.ts`): the panel was `overflow:hidden` + `maxHeight:40vh` with `nowrap`/ellipsis rows, so lower lines and long chat got clipped by the window. Now a flex column with a scrolling body (`minHeight:0; overflowY:auto`) and wrapping rows (`overflow-wrap:anywhere`); `toggle()` restores `display:flex`.
- State: **Green.** 314 tests (+6 in `metrics.test.ts` for the new functions: vow tally/mean-drive, dynasty largest-share, assortativity =1 when fully endogamous & zeros when unpartnered, occupation evenness). `tsc` clean. **No soak run** — all additions are read-only measurement/render that consume no sim RNG, so the trajectory is unchanged (determinism untouched). **Live-verified in the browser** (preview, no console errors): Ecology tab renders 29 grazers / 3 predators / "1 hunter per 10 grazers" + species rows + flora ripeness; all 11 emergent articles show descriptions + live values (e.g. dynasties "10% in the biggest line · 20 lines", cultural mixing "+0.24 · 63% marry within · cultures stay apart", variety of work "0.92 · 6 trades"); the feed body now scrolls (scrollHeight 546 > clientHeight 482) and wraps.
- Next: **M10 slice 4 — language as a mechanic** (fluency map, same-language synergy, gradual learning) + **Conversation/Language master tabs**; make agents *visibly* chat.
- Blockers/Questions: none.
- Decisions logged: none new (measurement-only, extends the M7.7/D29 instrumentation; the Ecology tab is the D27/D35 "every system ships a view" rule applied to M8 ecology).

## Session 41 — 2026-06-23 — M10 slice 3: memory & belief become causal + more emergent life events
- Did: Made the **inner life causal** (D26) and richer. New deterministic **`distill(memories)`** (`src/ai/memory.ts`, no LLM → replay-safe) weighs a life's memories by theme — **bonds** (`child|wed|born|befriend`) and **toil** (`work|prosper`) → striving; **grit** (`survived|overcame|pulled through`) → seizing the day; **loss** (`lost|ill`) → grief/withdrawal — and returns a bounded **`purpose`** (±0.4) + a named **`vow`** (now **5**: provide / live fully / guard / make something / take each day). `grit` is tested before `loss` so "survived a grave illness" reads as resilience, not just another loss. `AISystem.reflectPass` runs `distill` each reflection, writes `purpose`/`vow` onto the `Memory`, and emits a `decide` feed line when the vow turns over. **`ActionSystem`** reads `purpose` to bend the effective wealth goal (`goal = wealthGoal·(1+0.5·purpose)`) — a driven agent keeps working past where a content one stops. The LLM `beliefs` stream stays **pure recorded flavour** (kept the causal/flavour split). **More remembered life events** (which feed `distill` and the Town Happenings feed): **befriending** someone (`SocialSystem.interact`, new `friendship` event-kind + "befriended X" memory, fired on first crossing of `friendSentiment` — the edge default type is already `friend`, so the transition is detected by *sentiment* not type); **surviving a grave illness** (`HealthSystem`, gated by a new optional `Health.grave` flag → a "pulled through a grave illness" feed line + "survived" memory); and **outliving your own child** (a parent now grieves a dead child, importance 0.95 — the heaviest loss). The inspector Mind section shows a ⚑ vow line (added last edit). Feed got `friendship` colour/glyph (`⊕`).
- State: **Green.** 308 tests (was 303; +`test/mind.test.ts`: distill themes incl. grit-beats-"ill", purpose signs, ActionSystem purpose bias, friendship-memory integration). `tsc` clean; 40k soak **0 violations**, pop steady (folk=60, born=58, graves=22 — the new memories don't consume sim RNG, so the trajectory barely moved; `reflective=60`). Fixed one **brittle culture test** the slice-2 trajectory shift exposed: `a locally-born child shares its mother's culture` asserted exact-id equality, but a post-birth **schism** can break mother or child into a daughter culture (`root.d<tick>`) — relaxed to the true invariant (same family **root**, `id.split('.')[0]`).
- Next: **M10 slice 4 — language as a mechanic** (per-agent fluency map, same-language synergy, gradual learning) + **Conversation/Language master tabs**; make agents *visibly* chat (decouple the chatting badge from the rarely-low social need).
- Blockers/Questions: none. Note: only 3 of the 5 vows are common in a 40-year run (grit/loss need grave illness or child-death, which are rarer early); over deeper time they surface more.
- Decisions logged: none new (executes D26 — the distillate is causal & deterministic, the LLM beliefs stay off-trajectory flavour, exactly as D26 prescribes).

## Session 40 — 2026-06-17 — M10 slice 2: culture is fully causal (open → friendship, traditional → endogamy)
- Did: First **specced** how each value axis steers behaviour (new "How values steer behaviour (causal coupling — D26)" table in `CULTURE_AND_LANGUAGE.md` + refined the slice-2 roadmap bullet), then **wired it**. Two new `cultureStore` helpers: `bondFactor(a,b)` (same culture → 1; cross-culture → the pair's average `open`, floored 0.15) and `prefersEndogamy(c, roll)` (`roll < c.values.traditional`). `SocialSystem.interact` now scales the **friendship** sentiment gain by `bondFactor` — so company (the social *need*) is still met by anyone, but warmth only crosses cultures for **open** folk (insular folk stay socially segregated). `SocialSystem.matchmake` rolls `prefersEndogamy` and, for **traditional** folk, restricts to same-culture partners (falling back to any eligible if none). `martial` stays **deferred to M16** (no behavioural home until combat); `communal`→wealth-goal was already causal — so all four axes are now accounted for (3 causal, 1 deferred-by-design).
- State: **Green.** 303 tests (was 299; +4 `cultureCausal.test.ts`: `bondFactor` same/cross/floor/no-culture, `prefersEndogamy` always/never; a SocialSystem integration showing a same-culture pair warms faster than an open↔insular pair (both still bond); and a matchmaking test where a traditional male weds within his culture while an innovative one takes the first eligible). `tsc` clean; 40k soak **0 violations**, pop stable (folk=60). Trajectory shifted (the endogamy roll consumes RNG per marrying male + scaled sentiment), reproduction/properties still pass. Effect is behavioural (who befriends/marries whom) → verified by tests + soak, like the original `communal` causality.
- Next: **M10 slice 3 — memory & belief become causal** (distil beliefs/memories into numeric drives that bias action selection; generate more memories + vows; today beliefs are write-only flavour). Then slice 4 (language fluency/synergy + Conversation/Language master tabs).
- Blockers/Questions: none. Note: an emergent consequence to watch — insular + endogamous cultures (the Drakhan: open 0.20, traditional 0.80) self-segregate and, when few in number, struggle to find same-culture partners (they fall back to intermarriage, so no demographic collapse — the existing dwarf≈1 is from spawn weights, not this). That asymmetry (open cultures blend, insular ones stay distinct) is the intended emergent storytelling.
- Decisions logged: none new (executes D26 + the documented spec; couplings are small + soak-gated as D26 prescribes).

## Session 39 — 2026-06-17 — M10 slice 1: master tabbed view + UI fixes (sleep jitter, legend, building)
- Did: M10 slice 1. **Master tabbed view** (`src/render/masterPanel.ts`, hotkey **Tab** + per-view C/E/F/T/G jump to a tab, Esc closes): one modal holding Legends/Economy/Find/Family/Lineages as scrollable tabs. **Low-churn design** — added a public `content` getter to `ModalPanel`, a `update(world)` to each dashboard, refactored `LegendsPanel` from its own modal to a content element, and the master **reparents** each view's persistent element into a shared slot, showing/refreshing only the active one (so the directory search box + family-tree navigation keep their state across tab switches). `main.ts` rewired: dropped `toggleDashboard`/`anyDashOpen`/`closeDashboards`, registered the 5 tabs, hotkeys → `master.openTab`, refresh loop → `master.refresh`. The inspector stays the entity side-panel; Legend (L) + Town Happenings (H) stay overlays. **Quick fixes:** **sleep/work hysteresis** in `ActionSystem` (commit to sleeping-until-rested (energy ≥ 0.85) / eating-until-full unless the other survival need is urgent — fixes the every-other-tick jitter, which was threshold-thrash at the 0.4 gate); **legend** badges now a vertical scrollable list with "what it represents" descriptions (Child fixed); **building-icon** description generalised ("home, workplace, or civic place").
- State: **Green.** 299 tests (was 297; +2 `systems.test.ts` hysteresis: stays asleep when energy past the gate but not rested, wakes once rested). `tsc` clean; 40k soak **0 violations**, pop stable (folk=60), and folk are **more settled** with hysteresis (married 48 vs ~30, higher avgAge — less thrashing → steadier lives); reproduction/properties pass. **Live-verified (DOM):** the master view opens, all 5 tabs render their content (Economy WEALTH, Find search focuses + filters, Family, Lineages tongue-tree, Legends chronicle), tab switching works via keys + buttons, **no console errors**. (The screenshot tool timed out — the rAF render loop never idles for a capture — but evals are instant and the DOM is correct, so it's a capture-tool artefact, not an app bug.)
- Next: **M10 slice 2 — culture is fully causal** (wire `open`/`traditional`/`martial` axes to behaviour; only `communal`→wealth-goal is causal today). Then slice 3 (memory/belief→behaviour), slice 4 (language fluency/synergy + Conversation/Language tabs — these become master tabs).
- Blockers/Questions: none. Note: Town Happenings + Legend are still separate overlays (not master tabs) — fine for now; can fold them in later. Conversation/Language/Mind will register as new master tabs in slices 3–4.
- Decisions logged: none new (executes the M10 plan + D37; the reparenting master-host is a clean implementation choice, no architectural fork).

## Session 38 — 2026-06-17 — Planning: reprioritise M10 to "Causal & Legible Minds" (directed, docs-only)
- Did: A design chat (no code) after a review of the running sim. Diagnosed the **sleep/work jitter** (an agent whose energy settles on the 0.4 action threshold flips sleep→work every tick: one sleep tick +0.008 vs ~0.003/tick decay → thrash; fix is **hysteresis** — sleep until rested / eat until full). Confirmed from the code: **culture is barely causal** (only `communal`→wealth-goal; `martial`/`traditional`/`open` are unused), **beliefs/memories never feed behaviour** (write-only flavour, D19/D20), agents **do** chat (the `❝` dialogue on adjacency) but the "chatting" *badge* is rare (it's the `socialize` action, which only fires when the social need drops below 0.4 — adjacency keeps it topped up), and **conversation has no mechanical effect**. Saves live in `localStorage` (one slot `omnia.save`); the big-world load freeze is the replay-based O(ticks) reload. **Reprioritised the roadmap**: inserted **M10 — Causal & Legible Minds** (slice 1 = the **master tabbed view** + UI fixes (sleep hysteresis, legend vertical-list/scroll/descriptions, building-icon wording), then culture-all-axes-causal, then memory/belief→behaviour, then **language as a mechanic** — fluency %, same-language synergy, gradual learning — + Conversation & Language tabs), **M11 — Homes & Property** (build/own homes now), **M12 — Robust Save/Load & World Management** (snapshot loads, multiple named worlds, delete, disk export, IndexedDB). The old M10–M19 renumbered to **M13–M22** (M22 is now the closing UI/aesthetic polish; the master view + conversation tab moved into M10). Recorded **D37**.
- State: **Green** (297 tests; docs-only). Baseline unchanged.
- Next: **M10 slice 1** — the **master tabbed view** (a `TabHost` modal holding Legends/Economy/Directory/Family/Lineages as scrollable tabs; existing hotkeys jump to a tab; inspector stays the entity side-panel) **+ the quick UI fixes** (sleep/work hysteresis; legend vertical badges + scroll + "what it represents" descriptions incl. the Child wording; generalise the building-icon description). The master UI is the priority.
- Blockers/Questions: none — the three forks were answered (master UI first; save/load robustness is a later concern, my call on storage → IndexedDB + disk export when M12 lands; order = UI fixes + master UI first, then causal minds, then homes, then save/load).
- Decisions logged: **D37** (M10 reprioritised to Causal & Legible Minds; master tabbed view first; language becomes a real mechanic with fluency/synergy/learning; milestones renumbered).

## Session 37 — 2026-06-17 — M9 COMPLETE: YAML config loader (simulation.yaml is now authoritative)
- Did: M9 slice 3 (last slice). **`config/simulation.yaml`** — a flat 1:1 mirror of `defaultConfig` (generated from it, so it matches exactly) — is now **loaded at startup** as the authoritative runtime config: the browser via a Vite **`?raw` import** in `main.ts` (`vite/client` types already in tsconfig), the soak via `node:fs`. New `src/sim/configLoader.ts` `loadSimConfig(yamlText)`: parses YAML, **merges over `defaultConfig`** (so the YAML may be partial — omitted keys fall back), and **fails loud** on unknown keys / non-finite numbers (the content-loader ethos). `defaultConfig` is now the **typed fallback + validation schema**, not the runtime source. Removed the stale `docs/simulation.yaml` (it was grouped prose that had drifted from the flat `SimConfig` — extra/renamed fields); updated the README + `config.ts` comment to point at `config/simulation.yaml`.
- State: **Green.** 297 tests (was 294; +3 `configLoader.test.ts`: the shipped YAML loads to exactly `defaultConfig` (**drift guard** — a future `config.ts` change not mirrored to the YAML fails here), a partial YAML overrides only its keys, and unknown-key / non-number / non-mapping inputs abort). `tsc` clean (incl. the `?raw` import); 40k soak **0 violations**, identical to before (YAML === defaults). **Live-verified** the app boots from the YAML (newSimulation works, fauna/folk grow) with **no console errors**.
- **M9 DoD met (S35–S37):** start screen sets seed / population (10–100) / map size (up to ~20×); save/load is **byte-identical + continuable** (replay-based, localStorage UI); config is **YAML-authoritative** (loaded at startup, merged over typed fallbacks, fail-loud).
- Next: **Milestone 10 — Agent Depth: Body, Mind & Inventory** (D&D ability scores, dynamic 9-alignment, personality, one inherited Heredity system, inventory, status/HP) — all causal (D26) + legible (D35). First slice likely the **ability scores + one Heredity system** (generalising the existing heritable magic aptitude), or **alignment** (the most behaviourally visible). Pick one per the Continue Loop.
- Blockers/Questions: none. Follow-ups recorded in the M9 DoD: a state **snapshot** for instant loads (replay-load is O(ticks), slow for very long/Huge runs); and threading the **active cfg into the display helpers** that still read `defaultConfig` (inspector/dashboards age & calendar) so editing display-relevant YAML knobs updates those views too.
- Decisions logged: none new (executes the long-standing backlog item; merge-over-defaults + fail-loud follows D9/D14's content-validation ethos; no architectural fork).

## Session 36 — 2026-06-17 — M9 slice 2: deterministic Save/Load (replay-based)
- Did: M9 slice 2. New `src/sim/saveload.ts`: a **`SaveGame {version, savedAtTick, config, ai}`** is the save — the reproducibility unit (full config incl. seed + the current tick count + the recorded LLM responses). `buildSave` reads it (pure); `loadSave` **recreates the world from the config and replays forward `savedAtTick` ticks** with a `RecordedProvider` built from the saved AI log — which reproduces the **exact** state (positions, RNG position, beliefs/utterances) because the sim is deterministic and the soul replays from the record. `serializeSave`/`parseSave` are JSON with fail-loud validation. **UI:** the pause menu gained **💾 Save game / 📂 Load game** (persisted to `localStorage` under `omnia.save`; Load is greyed when no save exists; Save flashes "✓ Saved"). Replay is the **correctness baseline** (meets the DoD's "byte-identical"); a state **snapshot for instant loads** is the documented follow-up (replay-load is O(ticks) — fine for normal runs, slow for very long/large ones).
- State: **Green.** 294 tests (was 292; +2 `saveload.test.ts`: JSON round-trip + fail-loud on bad version/JSON; and **save→serialise→parse→load is byte-identical AND continuable** — snapshot equal immediately after load, and running the original + the reload forward 1500 ticks keeps them in lock-step, proving the RNG was restored exactly). `tsc` clean. **Live-verified** the full UI flow: ran to t=2000, Save, advanced to t=3000 (state changed), Load → restored **exactly** to t=2000 (identical agent name@pos), no console errors.
- Next: **M9 slice 3 — wire the YAML config loader** so `simulation.yaml` becomes the authoritative tunables (today `src/sim/config.ts` is authoritative and the YAML is reference-only) — the last M9 slice. Then M9 is done.
- Blockers/Questions: none. Note: load **replays** from the config, so loading a very long or Huge-map run can take a few seconds (it re-simulates to the saved tick); a state snapshot would make it instant and is the recorded follow-up.
- Decisions logged: none new (replay-based save/load follows the determinism architecture (D5/D19) and reuses the M7.7 manifest + AIRecord; no fork).

## Session 35 — 2026-06-17 — M9 slice 1: the World Setup screen (seed / folk / map size)
- Did: M9 slice 1. The start menu now collects a `SetupOptions {seed, population, mapSize}`: a seed field, a **Folk slider (10–100)** (the "tweak starting agents 10–100" request), and **map-size presets** — Small 64 / Medium 128 / Large 200 / Huge 288 (up to ~20× area), finally exposing the M8 big map **live**. `main.ts`'s `newSimulation(opts)` builds the cfg (`seed`, `initialPopulation`, `gridWidth=gridHeight=mapSize`) and calls the new **`renderer.configure(cfg)`** — the renderer's `cellSize`/`mapW`/`mapH` were construction-time constants, now re-derived so any map size renders, the camera clamps/fits, and click-to-inspect maps correctly. Restart and Settings-apply preserve the chosen population + map size (`lastSetup`).
- State: **Green.** 292 tests (render-only changes — the dashboards/menu aren't import-tested); `tsc` clean. **Live-verified:** the setup screen renders (seed / Folk slider / Small·Medium·Large·Huge presets, Small highlighted); booting **Large (200×200, 80 folk, seed 5)** generates an area-scaled world (folk→100, fauna 990, flora 3102) that **renders the whole Voronoi map at the correct tiny-cell scale**, camera fit to it, banner correct, **no console errors**.
- Next: **M9 slice 2 — Save/Load** (deterministic: the M7.7 run-manifest (seed+config) + the recorded event/AI log + a state snapshot; load restores exactly — replay as the correctness baseline, snapshot for speed). Then **slice 3 — wire the YAML config loader** so `simulation.yaml` is authoritative (long-deferred).
- Blockers/Questions: none. Note: the **Huge (288×288, ~20×)** preset will have a large eventual carrying capacity (folk cap ~1200, fauna ~2400) and grow slowly; perf was measured fine at 10× (3 ms/tick) — if 20× feels heavy once populated, the deferred LOD tier (D36) is the lever.
- Decisions logged: none new (executes the M9 plan; `renderer.configure` is a mechanical generalisation, no architectural fork).

## Session 34 — 2026-06-17 — M8 COMPLETE: predator–prey ecology (slice 5) + LOD deferred; foundations done
- Did: Closed out M8. **Slice 5 — ecology (the moth-grazer-flood fix):** fauna gained a `diet` (grazer/predator, content-driven, default grazer). **Predators** (ember_hound in the wastes, new **pallid_stalker** in plains/forest) hunt the nearest grazer within a **short sight range** via the spatial grid, and **fall back to grazing flora** when no prey is near. **Folk hunt** adjacent fauna when desperate (`hunger<0.25`) — a light cull + fallback food. The flat `maxFauna=150`/`maxPopulation=60` became **area-scaled carrying capacities** (`scaledMaxFauna`/`scaledMaxPopulation`), with grazers and predators on **separate caps**. **Slice 4 — LOD:** **deferred** (see below). This required **substantial tuning** — the naive predator–prey model is bistable (boom-to-700-carpet or bust-to-extinction); I worked through ~8 soak iterations to find a stable design (recorded as **D36**): density-dependent predation (sight-range refuge) + omnivorous predators (never starve out) + grazers winning the breeding race under their carrying cap + separate predator cap (~8%). Result: **stable ~120 grazers + ~10 predators on 64×64, verified across 6 seeds at 40k ticks** — no extinction, no carpet (down from the old static 150), predators visibly thin/chase the herds, and it scales with map area.
- State: **Green.** 292 tests (was 289; +3 `predation.test.ts`: a predator eats an adjacent grazer; a predator with no prey falls back to grazing (doesn't starve out); a 16k live-loop keeps fauna bounded — grazers>0, predators>0, total ≤ 1.2× carrying cap). `tsc` clean; 40k soak **0 violations**, fauna stable ~130, perf back to **15.5 s** (down from 38 s when fauna briefly carpeted at 700). Reproduction (seed 8) still surfaces a grandchild. **Live-verified** (seed 8, 9k steps): grazers 120, predators 10 (Pallid Stalkers), diverse fauna spread across biomes (not a carpet), no console errors.
- **M8 DoD met (S30–S34):** spatial index (S30) + A* (S31) + area-scaling big map (S32) + ecology/uncap (S34). **Measured a 10× map (200×200) at populated steady state (~1300 entities) running at 3 ms/tick (~330 ticks/s)** — within budget thanks to the sparse index + sparse A*, which is why **LOD is deferred** (not needed yet, and adding it would perturb the tuned ecology). Fauna self-regulate (no flat cap → area-scaled carrying capacity + predation; no swarm; no extinction across 6 seeds); A*-based; deterministic; soak green at scale.
- Next: **Milestone 9 — World Setup & Save/Load**: a setup screen (seed / starting population 10–100 / map size — the big-map slice's live UI), deterministic save/load (the M7.7 run-manifest + recorded events + a snapshot), and wiring the YAML config loader.
- Blockers/Questions: none, but a heads-up — **"uncapped" landed as an area-scaled ecological carrying capacity, not literally unbounded** (D36): a simple mobile predator–prey model is bistable (boom-carpet or bust-extinct), so a scaled carrying cap + density-dependent predation is what gives a stable, dynamic, non-carpet population. If you want wilder boom-bust swings instead of a steady level, say so and I can dial the predation/refuge toward oscillation (at some stability risk).
- Decisions logged: **D36** (area-scaled ecological carrying capacity + density-dependent predation; LOD deferred until the budget needs it; refines D32).

## Session 33 — 2026-06-17 — Requested UI fixes + flora/fauna diversity (M18/M19 pulled forward)
- Did: A focused UI + content pass on the punch-list (mostly M19 UI + M18 content, pulled ahead of M8 slices 4–5 by request). **Time/banner:** new legible calendar — `calendarOf()` shows **Year · Season · Month** (cosmetic subdivision of the aging year so dates & ages still agree), replacing the confusing cumulative "Day N". Top banner is now minimal — day/night ☀/☾, the date, **Folk**, and **real-world watch-time** (⏱, tracked in `main.ts`, excludes paused/menu); Gini/Mages/Graves/Fauna moved off. **Speed** slider is now **exponential up to 1000 ticks/s ≈ 1 sim-year/second** (was linear ≤100), and the loop's ticks/frame cap raised 30→500 so top speed actually lands. **Zoom %** is labelled and moved to the bottom-right corner. **Bug fixes:** the inspector **✕ now closes** the card (root cause: `update()` rebuilt the whole panel — incl. the button — every frame, destroying it mid-click; fixed with a persistent ✕ + body, Esc also closes); the **Finder no longer types "f"** into its search field (`preventDefault` on the letter hotkeys). **Controls** moved off-screen into **Esc → Controls** (removed the `#hud` strip; one keymap source in `menu.ts`). **Town Happenings** is hideable with **H** (like Legend's L). **Legend** expanded to describe every symbol — incl. the `|||` = *seeking food* badge that was asked about — and shows the **child folk at actual reduced scale** (not "child = smaller" text). **Gini** explanation added to the Economy tab. **Icons:** redrew the crude **animal** (clean quadruped) and **timber** (log-stack with growth rings); fauna are now tinted by their **own colour + size** on the map (renderer was drawing every animal as one amber blob). **Content diversity (the "tired of moth grazers" ask):** +6 flora (→10) and +6 fauna (→8), distributed so each of the 4 passable biomes has a distinct ecology (the crystal flats now have life; the wastes have cinderbuds + beetles + a wary ember-hound).
- State: **Green.** 289 tests (relaxed one brittle assertion in `stats.test.ts`: the Chronicle's recent-entries count is bounded but compression is *scheduled* off the hot path, so it can sit a few past the cap between passes — the new content's trajectory shift surfaced 11 vs the asserted ≤10; now ≤cap+10, which is the real "bounded, not exact-capped" invariant). `tsc` clean; 40k soak **0 violations**, pop stable. Reproduction (seed 8) still surfaces a grandchild despite the world-gen RNG shift from the new content. **Live-verified** every fix in the browser (seed 3/8): banner/date/⏱, Zoom label, inspector ✕ closes, Finder search empty, H toggles Happenings, Esc→Controls shows the keymap, speed maxes at 1000 ticks/s; map shows **6 fauna + 10 flora species** with varied colours/sizes; **no console errors**.
- Deferred (recorded in ROADMAP): the **master tabbed view** (one panel, all views as tabs) — a sizeable refactor of the 5 dashboards, its own focused pass; the broader inspector overhaul, conversation tab, races, monsters/special agents, and the full icon library remain in M18/M19.
- Next: **M8 slice 5 — uncap populations → ecological limits**: remove the `maxFauna`/`maxPopulation` hard caps and add **predators + agent hunting** so fauna self-regulate (the moth-grazer-flood fix) — the new `ember_hound` is the natural first predator. (Direction: skip slice 4 LOD for now, go to slice 5 after these fixes.)
- Blockers/Questions: none. Note: the time **compression** (D16, 4 aging-days/year) means a "year" is short; the calendar shows seasons/months as cosmetic subdivisions rather than a 365-day year, since lengthening the year would unbalance every per-day rate. Flagging in case you want a different calendar feel.
- Decisions logged: none new (UI/content execution under D35's legibility gate; no architectural fork).

## Session 32 — 2026-06-17 — M8 slice 3: the big, configurable map (world-gen scales with area)
- Did: M8 slice 3 — made the map size genuinely scalable. World-gen quantities that were absolute magic numbers (biome seeds, flora/fauna caps, employer count) now **scale with map area**: new `areaScale(cfg)` = (gridW×gridH)/(64×64) and `scaledBiomeSeeds`/`scaledMaxFlora`/`scaledMaxFauna`/`scaledBusinessCount` in `config.ts`, applied in `world.ts` (biome seeds + business count), `populate.ts` (initial flora/fauna caps), `FloraSystem` (spread cap), `FaunaSystem` (breed cap). **Identity at 64×64** (areaScale=1 → round(base)=base), so the default world is byte-unchanged. Also moved `FaunaSystem`'s nearest-ripe-flora search off its `ripeList` linear scan onto the `SpatialGrid` (behaviour-preserving, insertion-order tie-break) so fauna grazing scales on a big map. Confirmed the renderer is already size-agnostic (derives `cellSize`/`mapW`/`mapH` from `cfg.gridWidth/Height`; camera clamps to the map). **Folk counts (initialPopulation/maxPopulation) deliberately NOT scaled** — large populations are the LOD + uncap slices.
- State: **Green.** 289 tests (was 287; +2 `bigmap.test.ts`: a 200×200 (~9.8× area) world has >4× the base map's flora/fauna/resources, employer count == `scaledBusinessCount`, >1 biome present; agents spawn on passable in-bounds tiles and the world stays valid — agents in-bounds + on passable terrain — after 400 ticks, in <1s). `tsc` clean. **Default 64×64 soak byte-identical** to the post-A* reference (yr=38 folk=60 [dwarf=14 human=46] … gini=0.81, science block matching), confirming area-scaling is a no-op at base size. Big-map run is fast (200×200 × 400 ticks <1s), so world-gen + the sparse grid/A* substrate scale fine.
- Next: **M8 slice 4 — LOD brain tiers (D32):** foreground/near agents fully simulated, distant/background agents a cheap deterministic approximation, so a big map can hold a large population within the per-tick budget. Then **slice 5 — uncap populations → ecological limits** (remove maxFauna/maxPopulation hard caps; add predators + agent hunting so fauna self-regulate — the moth-grazer-flood fix). After M8, the big map gets *filled* (folk scaling) and exposed via the M9 setup screen.
- Blockers/Questions: none. Note: the big map is capability-complete + tested headless, but not yet reachable in the live UI (the default stays 64×64); **M9's setup screen** is where you'll pick the size and see it. Scaling fauna count up (scaledMaxFauna) means a big map currently has *more* moth grazers until the ecology fix (slice 5) makes them self-regulate — expected, not a regression.
- Decisions logged: none new (executes the M8 plan under D35's engineering bar; area-scaling identity-at-base preserves determinism).

## Session 31 — 2026-06-17 — M8 slice 2: A* pathfinding (agents route around terrain)
- Did: M8 slice 2 — replaced greedy `stepToward` with **A\*** for sapient movement. New `src/sim/pathfinding.ts`: `findPath` (deterministic 4-neighbour A*, Manhattan heuristic, a small binary-heap open set with ties broken by (f, h, tile-index), sparse `Map`/`Set` keyed by tile index so cost scales with the area explored rather than the whole map, an expansion budget) + `pathToward` (takes one step along the route; if the next planned tile is momentarily occupied or no route exists, falls back to the greedy `stepToward` for local avoidance/wander — so dynamic-collision behaviour and its RNG use are unchanged). **Planning is over static terrain only** (the `Enterable` gate); occupancy is a step-time concern, so paths don't thrash as crowds shift. Wired `pathToward` into `MovementSystem`'s four movement calls (forage / commute-to-employer / gather-node / socialise); **fauna keep cheap greedy stepping** (instinct tier — no pathfinding). The spatial index from S30 supplies the targets; A* gets agents *to* them around bays/walls greedy would stick on.
- State: **Green.** 287 tests (was 281; +6 `pathfinding.test.ts`: empty path at goal, shortest path on open terrain = Manhattan length with unit orthogonal steps, **routes around a wall** a greedy stepper sticks on, null when walled-off / goal-unenterable, null past the expansion budget, determinism). `tsc` clean. **Soak:** stable to cap (60), **0 violations**, and only **+0.4s/40k ticks** (16.2s vs 15.8s) — per-tick A* is affordable on the open map (strong heuristic; sparse exploration), so no path-caching needed yet. Trajectory **shifted** (smarter routing changes who reaches which food/partner when → different downstream RNG draws), still fully deterministic — and notably the `reproduction` multi-gen (seed 8) + `properties` identical-trajectory + `soul`-replay tests all passed **without seed re-tuning**.
- Next: **M8 slice 3 — the big, configurable map** (target 10–20× area; world-gen, camera, renderer scale to it). With the spatial index (sparse) + A* (sparse, budgeted) already in place, the substrate is ready; the big map will want **coarser spatial-grid cells** and an **A* expansion budget / path caching** to stay within the per-tick budget (both are drop-in — the APIs don't change). Then LOD brain tiers, then the uncap-population/predators-hunting ecology fix.
- Blockers/Questions: none. Note: target *selection* is still Manhattan-nearest (the spatial grid), so an agent can still pick a Manhattan-near but across-water target and A* then fails → greedy fallback (same as before). Reachability-aware selection (pick the nearest *reachable* target) is a possible follow-up but costs more A* calls; deferred until it matters.
- Decisions logged: none new (A* was specified in the M8 plan; clean implementation under D35's engineering bar).

## Session 30 — 2026-06-17 — M8 slice 1: the spatial index (perception substrate)
- Did: Started M8 (Foundations II) with its first, foundational slice — the **spatial index**. New `src/sim/spatialGrid.ts`: a tile-granular hash grid (`Map<tileKey, entries[]>`) with `nearest(x, y, accept?)` (expanding square-ring search that returns the **global-min Manhattan** entry, **ties broken by insertion order**), `within(x, y, radius)` (range query for clusters/AoE later), and `at(x, y)`. Wired it into `MovementSystem`, replacing the **three per-tick O(n) linear scans** (nearest ripe flora to forage / nearest other folk to socialise with / nearest non-empty resource node to gather) with grid queries — building one grid per category per tick from the same component-query order, so the insertion-order tie-break **reproduces the old scans exactly**. This is the perception/movement substrate the rest of M8 (A*, big map, LOD, combat targeting) builds on.
- State: **Green.** 281 tests (was 273; +8 `spatialGrid.test.ts`: insert/at/size/clear; nearest = null-when-empty / Manhattan-not-Chebyshev / **ties-by-insertion-order** / predicate / far-point-clipped-to-map / **brute-force equivalence over 300 queries × a predicate variant**; within = inside-radius-only). `tsc` clean. **Behaviour-preserving refactor proven:** the 40k soak signature is **byte-identical** to the pre-change reference (yr=38 folk=60, gini=0.83, wealth/social/names science block all identical, 0 violations), and the determinism (`properties` identical-trajectory) + `reproduction` + `soul`-replay tests pass **without any seed re-tuning**. So no trajectory shift — a transparent perf substitution.
- Next: **M8 slice 2 — A\*** pathfinding (replace greedy `stepToward` in `movementUtil`), using the spatial index + a movement/perception budget. Then the big configurable map, LOD brain tiers, and the uncap-population/predators-hunting ecology fix. (Order within M8 is flexible; A* next is natural since the grid is now in place, but the fauna ecology fix is the fastest *visible* win if a demo is wanted.)
- Blockers/Questions: none. Note: the grid is tile-granular (cell=1) for now — exact + simple; coarser cells are a drop-in perf optimization for the big-map slice (the `SpatialGrid` API won't change).
- Decisions logged: none new (a clean perf refactor under D35's engineering bar; no architectural fork).

## Session 29 — 2026-06-17 — Planning: the M8–M19 "living-world arc" (vision pass, docs-only)
- Did: A design/planning session (no code) turning the large scratch-notes/vision dump into a structured, ordered backlog. Synthesised the wishlist (D&D-style agents: ability scores/alignment/personality/inherited traits/inventory/status; tribes/factions/governments/gangs; markets/homes/landlords/trade; combat/hunting/crime/vice/war; science **and** magic tech trees + research + achievements; religion; events/seasons/disasters/paranormal; historical figures/dynasties/artifacts/quests/wonders; a 10–20× world; save/load; a real bestiary of races/animals/monsters/special-agents/buildings; an inspector/UI overhaul; plus fixes: fauna swarm, top-bar, controls→Esc, finder bug) onto **a few reusable engines**, and wrote **Milestones 8–19** into `ROADMAP.md` with Goal/slices/DoD each, preceded by standing rules. Locked three forks via a question prompt: **foundation-first ordering**, the **full sci-fi tech ceiling** (framed as re-ascending the post-apoc world's lost tech, per D8), and a **level-of-detail (LOD)** scale model with **no artificial population caps**. Recorded **D32–D35** and added a "living-world arc" section + two non-goal reconciliations (scale; procedural-vs-authored quests) to `VISION.md`.
- The arc (foundation-first): **M8** Scale/Perception/Ecology (spatial index + A*, big map, **LOD tiers**, uncap pop → predators+hunting fix the fauna flood) → **M9** Setup & Save/Load (start screen pop 10–100; save = M7.7 manifest + recorded events + snapshot; wire the YAML config loader) → **M10** Agent Depth (stats, dynamic 9-alignment, personality, **one Heredity system**, inventory, status) → **M11** Institutions (the unified **`Organization`** engine; tribes/factions/governments; procedural faction colours; reputation; schism-with-culture/language) → **M12** Economy Depth (supply/demand market, homes/landlords, craft coupling, debt rebalance) → **M13** Conflict (D&D combat, hunting, wounds, crime & vice, war) → **M14** Knowledge (tech **and** magic trees, research by org, full sci-fi ladder, lost-tech/rediscovery, achievements, **mages made legible**) → **M15** Religion → **M16** Events/Seasons/Paranormal (content-driven event pipeline) → **M17** History/Legend/Quest (historical figures, dynasties, artifacts, archaeology, procedural quests, wonders) → **M18** Content & Bestiary (4 more languages, common races, real animals, D&D monsters, special agents, buildings, icon library) → **M19** UI/Inspector/Legibility overhaul (conversation tab, help/guide, controls→Esc, minimal top-bar, finder-bug fix). Each milestone ships its own content + inspector view (legibility gate, D35).
- State: **Green** (273 tests; docs-only, no code). Baseline unchanged from S28.
- Next: **Milestone 8 — Foundations II: Scale, Perception & Ecology.** First natural slice: the **spatial index** (it unblocks A* perception, the big map, LOD, and combat targeting) — or, if a quick visible win is wanted first, the **fauna ecology fix** (predators + hunting + uncap) which directly addresses the "map crawling with moth grazers" complaint. Pick one slice per the Continue Loop.
- Blockers/Questions: none — three forks answered (foundation-first / full sci-fi ceiling / LOD scale). Note: D32 **revises D2** (scale) and **extends D10** (a new LOD brain tier); D33 mirrors D11 (one unified engine); D34 extends D11 (capabilities-as-content); D35 elevates D27 (legibility) to a hard gate. These are deliberate directed changes, not silent reversals.
- Decisions logged: **D32** (LOD scale, ecological caps), **D33** (unified `Organization`), **D34** (tech/magic trees, full sci-fi ceiling, research-by-org), **D35** (legibility gate + procedural-causal behaviour + engineering bar).

## Session 28 — 2026-06-16 — M7.7 COMPLETE: run manifests/CSV/diff + metrics surfaced in the Legends view
- Did: Closed out M7.7 (the milestone was to be finished before moving to M8) with the last two bullets. **Export/diff** (`src/analysis/manifest.ts`): a **run manifest** `{version, ticks, config(incl. seed)}` is the reproducibility unit — stable (key-sorted) JSON `serializeManifest`/`parseManifest` (fail-loud on bad version/missing config/non-numeric seed), `runManifest` rebuilds+runs+measures a manifest, `statsToCSV` exports the world-health time-series, and `flattenMetrics`+`diffRecords` give a run-diff. `npm run export` (`exportMain.ts`) writes `runs/seed8.manifest.json` + `runs/seed8.stats.csv`, **re-runs the manifest and proves 0 metrics changed (EXACT)**, then diffs seed 8 vs seed 1; `runs/` gitignored. **UI surface** (bullet 4): the Legends view (C) gained an **"Emergent structure"** section rendering `measureWorld` live — wealth Gini + power-law α (r²), friendships-per-person + clustering, small-world σ, surname Zipf ("≈ Zipf's law") vs flat given names, language-family depth/breadth (pure read; D27 legibility). New `npm run export` script; `docs/FINDINGS.md` updated with the manifest-reproducibility section.
- State: **Green.** 273 tests (was 265; +8 `manifest.test.ts`: serialise/parse round-trip, byte-stable regardless of key order, fail-loud on bad version/config/seed/JSON, **runManifest reproducibility** — a direct run and a serialise→parse→run are metric-identical (the DoD clause), a different seed diffs non-empty, `statsToCSV` header+rows, `diffRecords` changed-keys/eps/missing-as-0). `tsc` clean. **No `src/sim` changes** → determinism/soak untouched (still 0-violation from S26). **Live-verified** (dev server, seed 8, 9k steps): Legends → "Emergent structure" shows Gini 0.64 / power-law α 5.18 (r²=0.95) / friendships 17.6 clustering 0.75 / small-world σ 1.34 / surname Zipf 0.77 (r²=0.86, "≈ Zipf's law") / given 0.00 flat / 2 tongues — **no console errors**; `npm run export` prints "re-running the manifest changed 0 metrics → EXACT ✓".
- **M7.7 DoD met (done S26–S28):** a documented analysis (`docs/FINDINGS.md`) with **≥1 emergent regularity** (surname Zipf) **and ≥1 located phase transition** (food scarcity, `floraDensity ≈ 0.0175`), **reproducible from an exported manifest** (`npm run export` → byte-exact re-run), metrics surfaced in-app. The science track is now a real instrument: measure (`metrics.ts`/soak), sweep (`sweep.ts`), export/diff (`manifest.ts`), see (Legends).
- Next: **Milestone 8** — Economy Depth, Building, Social Structures, Vice, UI & Refinement. It's queued to start. First natural slice (per the M8 list): a supply/demand **market** (prices move with supply/demand; agents buy food/goods; business revenue from real sales; bankruptcies) — completes M3's deferred economy. Pick one M8 bullet per the Continue Loop.
- Blockers/Questions: none. M8 is broad/open-ended — worth a quick steer on which slice to start (market vs building vs conflict/vice), but the roadmap order suggests the market first.
- Decisions logged: none new (executes D29 under D31's pure-read/orchestration discipline; the manifest-as-reproducibility-unit is documented in code + FINDINGS, not a contested fork).

## Session 27 — 2026-06-16 — M7.7 (part 2): parameter sweeps locate a food-scarcity phase transition
- Did: M7.7 bullet 2, **Sweep**. New `src/analysis/sweep.ts` — pure orchestration over the existing headless loop (no new sim behaviour, no renderer): `runScenario` runs one (config, seed) to an outcome (final/peak population, survived, extinction tick, fauna), sampling population cheaply and stopping early on extinction (a town at 0 can't recover); `sweepParam` varies one config knob across values × seeds and aggregates survival rate + mean populations; `findTransition` brackets adjacent points where the survival rate crosses 0.5 and **linearly interpolates the tipping point** (picking the sharpest crossing). `npm run sweep` (`sweepMain.ts`) prints a survival/order-parameter table + the located transition. New `docs/FINDINGS.md` writes up the science-track results so far. (No `DECISIONS` entry — this *executes* D29 and follows D31's pure-read discipline; the only methodological choices, documented in code/FINDINGS, are: survival as the order parameter, food-supply `floraDensity` as the canonical knob, and the interpolated 0.5-crossing as the transition estimate.)
- State: **Green.** 265 tests (was 259; +6 `sweep.test.ts`: `findTransition` interpolates a crossing / returns null with no crossing / picks the sharpest of several; `runScenario` — a fed town survives + extinctionTick null, a flora-less town starves to extinction with a recorded tick; and the end-to-end `sweepParam` food-scarcity sweep — survival flips 0→100% as food rises, mean population is monotone in food, a transition is located inside the range). `tsc` clean. **No sim/loop changes**, so determinism/soak are unaffected (the 40k soak stayed 0-violation in S26 and nothing under `src/sim` changed).
- **Finding (the milestone's other half):** a **food-scarcity survival phase transition at `floraDensity ≈ 0.0175`** (seeds 1/2/8, 4000 ticks): 0% survival below it, 100% above ~0.04, mean surviving population rising from 0 like an order parameter (0/0/0/0/0/1.3/23.7/32.3 across the swept densities). A dual `hungerDecayPerDay` sweep erodes carrying capacity then collapses survival at ≈ 11/day. Near the boundaries seeds disagree (finite-size fluctuation in a town of dozens), which is honest physics, not a method bug.
- **M7.7 DoD status:** now have **both** required pieces — ≥1 emergent regularity (S26 surname Zipf) **and** ≥1 located phase transition (S27 food scarcity). The remaining clause is "reproducible from an **exported manifest**", which is the next bullet (Export/diff = run manifests of seed+config). After that, surface key metrics in the Legends/charts views, then M7.7 closes.
- Next: **M7.7 bullet 3 — Export/diff**: run manifests (seed + config → reproduce a run exactly), event-log/CSV export, and a run-diff. This is the clause that finishes the milestone DoD. Then **bullet 4 — surface key metrics in the Legends/charts views**.
- Blockers/Questions: none.
- Decisions logged: none new (executes D29 under D31's discipline).

## Session 26 — 2026-06-16 — M7.7 (part 1): the measurement module — emergent structure, quantified
- Did: Started M7.7 (Science & Instrumentation, D29) with its first bullet, **Measure**. New `src/analysis/metrics.ts` — pure functions over **durable state only** (living agents, tombstones, the runtime language store), **no RNG, no mutation, no coupling** (the D22 strata discipline, recorded as **D31**), so measuring a run can never perturb its deterministic trajectory and a seed measures identically every time. Metrics: **wealth** Gini + a **Hill power-law tail** estimator (α, threshold, tail r²); **social network** over the friendship graph — mean local **clustering** C, mean **shortest-path** L within the largest component (BFS), connected-component count, and a **small-world σ** vs an Erdős–Rényi baseline; **age distribution** (median/mean/child-fraction/decade buckets); **Zipf** fit (log-log rank-frequency regression) over given names & surnames; **language-family shape** (depth/breadth/roots/living-vs-lost). One `measureWorld()` bundles them. Wealth is measured as **gold held** (matches the HUD/strata/soak Gini). Demonstrated headlessly: the **soak now prints a "Science" block** of the measured structure at the end (pure read; no trajectory change).
- State: **Green.** 259 tests (was 246; +13 `metrics.test.ts`: `linregress` recovers a known line; `powerLawTail` fits α≈2 to a clean rank-size law + heavier-tail→smaller-α + too-few-points guard; `socialMetrics` exact on a triangle / path / disconnected graph + detects σ>1 on a rewired ring lattice; `ageDistribution` median/mean/child-fraction/buckets; `zipfFit` recovers s≈1 + `nameFrequencies` tallies living+buried; `languageFamilyShape` depth/breadth/roots/extinct; and an end-to-end `measureWorld` over a 10k live run). `tsc` clean; 40k soak **0 violations**, pop→cap.
- **Findings (the point of the milestone):** (1) **an emergent statistical regularity** — patrilineal **surnames are Zipfian** (s≈1.15, r²≈0.86, vocab 20) while **per-id given names are flat** (s≈0, vocab 75): inheritance manufactures concentration, generation-without-inheritance doesn't — the contrast validates the measure. (2) The **friendship graph saturates near-complete** (⟨k⟩≈48 of 60, C≈0.87, L≈1.19, **σ≈0.97 — *not* small-world**) at the current sentiment balance (friendships warm and never cool past the threshold). Recorded as a tuning lead, **not acted on** (changing it would shift the trajectory — out of this bullet's scope).
- Next: **M7.7 bullet 2 — Sweep** parameters to locate a phase transition (e.g. the population-collapse or overgrazing threshold) — the other half of the milestone DoD. Then **bullet 3 — Export/diff** (event-log/CSV, run manifests = seed+config, run-diff) and **bullet 4 — surface key metrics in the Legends/charts views**. The milestone DoD (a documented analysis with ≥1 emergent regularity *and* ≥1 phase transition, reproducible from a manifest) closes after Sweep + Export.
- Blockers/Questions: none. Note: small-world σ<1 is a real measured property of the social model, not a bug; if the dense friendship graph is undesirable later, add sentiment decay / an edge cap (a deliberate, soak-gated change).
- Decisions logged: **D31** (measurement = pure decoupled read of durable state; wealth-as-gold; soak is the headless analysis tool).

## Session 25 — 2026-06-16 — M7.5 COMPLETE: the live model (Ollama), opt-in, off the hot path
- Did: Wired the **live local model** through the existing `AIRunner` as an **opt-in** mode (D28); the deterministic stub stays the default for CI/headless/replay. **`AISystem` rewritten** around a single sync/async **`dispatch`**: with a sync provider (stub/recorded) it applies inline exactly as before (byte-identical); with an async provider (Ollama) it **submits the prompt to a per-world `AIRunner` singleton** (new `C_AIRUNNER` component) carrying a deterministic **stub fallback**, and on a **later tick drains + applies + records** the finished result (keyed by prompt hash, so `RecordedProvider` replays it through the sync path). Throttle is set at submit-time so an in-flight agent never re-submits; the 4 passes (reflect/dream/decision/dialogue) share one eligibility/prompt/apply path. Drain runs at the top of each async tick; an 8s timeout (`aiTimeoutMs`) + failed-fetch both fall back to the stub, so a slow/dead model **never stalls the tick** (invariant #4). **UI:** Esc → Settings → **🧠 AI soul: Stub / Live model (Ollama)** (`render/menu.ts`, applies on restart); `main.ts` picks `OllamaProvider` vs `stubProvider` per the toggle. New config `aiConcurrency=2`/`aiTimeoutMs=8000`. README gained a "Running with a live model (optional)" section (install Ollama, `ollama pull llama3.2`, the toggle, off-hot-path/recorded/fallback semantics).
- State: **Green.** 246 tests (was 243; +3 `asyncSoul.test.ts` with a `MockAsync` no-`completeSync` provider: submit→flush→drain applies+records on a later tick / the recorded async run **replays identically via `RecordedProvider`** / an in-flight agent doesn't re-submit). `tsc` clean; 40k soak **0 violations**, pop→cap, `cultures=3 tongues=3 drifts=5`. **Live-verified the no-Ollama graceful path** (default seed, live toggle on, 25×50 interleaved steps): provider `ollama`, async runner created, the failing localhost:11434 fetches fall back to the stub and the system **drained+applied+recorded 164 responses off the hot path** (12 souls with beliefs, 34 with utterances), **no stall, no console errors**.
- **M7.5 DoD met:** async submit→drain→apply→record→replay proven (unit) and live (fallback); sync path unchanged (all prior AI/soul tests pass); records & replays identically; with the stub default nothing changes; never blocks the tick. M7.5 done in one session (S25).
- Next: **M7.7 — Science & Instrumentation** (D29): measure emergent structure (wealth/Pareto fit, social-network metrics, demographic curves, Zipfian word/name frequency, language-family shape), parameter sweeps to locate phase transitions, run export/diff with manifests, and surface key metrics in the Legends/charts views.
- Blockers/Questions: none. **Deferred (recorded in M7.5's ROADMAP entry):** LLM *glossing* of coined daughter-tongue / value names — the procedural naming already works; LLM glossing would hook the `EvolutionSystem` and is a flavor follow-on, not a blocker.
- Decisions logged: none new (S25 executes D28, recorded S19).

## Session 24 — 2026-06-16 — M7 COMPLETE: slice 5 (compression + the lineages lens) + 3 reported fixes
- Did: Closed out M7. **Compression of the dead** (`src/culture/lineage.ts`, `compressLineages`, scheduled in `EvolutionSystem`): a culture with no living members / a tongue with no living speakers is marked **extinct** (kept as a compact descent record; daughters that die out get a "faded/lost" Chronicle legend), and the oldest fully-dead side-branches are **pruned** past `maxLineages` (24) — living lineages + their ancestry always kept, so object counts stay bounded over deep time. `RuntimeCulture`/`RuntimeLanguage` gained `extinct`/`diedTick`. **The legibility lens** (`render/lineagesDashboard.ts`, hotkey **G**): renders the language & culture **family trees** (indented by descent), living vs lost, speaker/member counts, and the **same name key in every tongue** so the sound drift is audible down the tree. **Reported fixes:** (1) the **Tongue tag was stale** after a schism — now derived **live** from the agent's culture's language (removed the denormalized `Agent.tongue`); a Lopo-kin member now reads "Tongue Lopo", not "Old Vant". (2) The on-screen **help text overlapped the speed bar** — moved it up (bottom 64px, z-index). (3) The **speed dial now goes to 100×** (slider max 40→100, wider). Soak prints `tongues=N(M lost)`.
- State: **Green.** 243 tests (was 241; +2 `lineage.test.ts`: extinction marking+legend, prune bounds the store while keeping living lineages). `tsc` clean; 40k soak **0 violations**, pop→cap, `cultures=3 tongues=3` (a schism + its dialect persist), object counts bounded. Verified live (default seed, 30k steps): the lens shows Old Vant → Lopo/Vaip with audible drift (same key → "Latlu"/"Utnev"/"Toko"; Drakhan "Zarogthurk"); inspector Tongue updates to the daughter dialect; all 5 dashboards open/close clean; 25 agents inspect without error; 0 agents missing a culture; HUD no longer overlaps the speed bar; speed maxes at 100×; **no console errors**. (Preview note: a stale dev server was squatting port 5173 — killed PID, restarted clean.)
- **M7 DoD met:** language family tree + cultural schism over deep time; culture measurably changes behaviour (wealth goal); all of it visible in-app (Chronicle legends, inspector tongue/culture/descent, the lens); within budget; tenability tested (prune + cap). M7 done across S19–S24.
- Next: **M7.5 — Live Model Integration** (D28): drive `OllamaProvider` async via `AIRunner` as an opt-in mode (stub stays the deterministic default), apply results across ticks, record for replay — the biggest lever on the *artistic* ceiling. Then M7.7 (Science & Instrumentation, D29).
- Blockers/Questions: none. Note: `schismChancePerEra` is 0.7 — at the default seed Vant-kin spawned ~2–3 daughters over 42 years (visible & bounded, but can dial down if it feels busy). Deferred M7 follow-ons recorded in the ROADMAP (event-driven value drift, contact/borrowing, a values-over-time chart).
- Decisions logged: none new (slice 5 executes the M7 plan + D27).

## Session 23 — 2026-06-16 — M7 slice 4 COMPLETE: divergence → family trees & schism
- Did: Cultures and languages now **diverge into families**. `forkLanguage` (in `languageStore`) coins a daughter dialect — a freshly-generated name that still *sounds* like the parent (via `word()`), a `parent`/`foundedTick` descent link, and a couple of immediate sound changes so it's already distinct (`RuntimeLanguage` gained the descent fields). `forkCulture` (in `cultureStore`) forks a tighter daughter sect tied to that new tongue, values nudged apart. New `maybeSchism` (`src/culture/schism.ts`): once per era a culture with ≥ `minSchismMembers` and a per-era `schismChancePerEra×(1−cohesion)` chance **fractures** — the breakaway faction (upper-id half) is reassigned to the daughter culture. Wired into `EvolutionSystem` after drift; each schism emits a `culture` event + a **Chronicle legend** ("The Vant-kin schismed: the Lopo-kin broke away…"). Inspector shows a culture's descent ("Lopo-kin ⟵ Vant-kin"). Also moved the evolution clock init to 0 so the seed state holds for the first full era. Config: `schismChancePerEra`/`minSchismMembers`/`schismValueNudge`.
- State: **Green.** 241 tests (was 234; +7 `schism.test.ts`: forkLanguage descent+divergence+determinism, forkCulture, `maybeSchism` builds both trees / reassigns the faction / deterministic, and a live-loop forced schism recorded as a Chronicle legend). `tsc` clean; 40k soak **0 violations**, pop→cap, `cultures=3` (a schism fired and persists), `drifts=5`. Tuned `schismChancePerEra` 0.3→0.7 so ≥1 schism reliably appears over ~8 eras across seeds (self-limited by the member gate; math: need ~0.7 for ~95% over 8 eras). Verified live (seed 2, 40k steps): **Old Vant grew a family — daughters Lopo / Vaip / Mikse** (cultures Lopo-kin / Vaip-kin / Mikse-kin), all with `parent` links; the Chronicle holds the schism + sound-shift legends; inspector shows the descent; the open Vant-kin fragmented while the insular Drakhan held together (emergent from cohesion); no console errors.
- Next: **M7 slice 5 — compression + the legibility lens** (the LAST M7 slice): dead tongues/cultures (no living members) compress to compact **descent records** (like tombstones); build a **"lineages of tongues & cultures" view** so the player can browse the family tree, a name's then-vs-now, a culture's values over the years. Then M7 is done.
- Blockers/Questions: none. Note: at the default seed Vant-kin schismed 3× over 40k ticks — a touch frequent, but each is a real lineage and it's bounded/self-limited; the asymmetry (open culture fragments, insular holds) is a nice emergent property. Can dial `schismChancePerEra` down later if it feels too busy.
- Decisions logged: none new (slice 4 executes the M7 plan).

## Session 22 — 2026-06-16 — M7 slice 3 COMPLETE: the evolution engine (tongues & cultures drift)
- Did: Languages + cultures became **live, mutable runtime stores** so they can evolve. New `LanguageStore` singleton (`src/lang/languageStore.ts`, seeded deep-clone from content) joins the `CultureStore`; **spawn now resolves the tongue from the runtime store**, so a drifted language names its later-born children differently. New scheduled **`EvolutionSystem`** (`evolutionIntervalDays` ≈ 5 sim-years, off the hot path, deterministic via the sim RNG): each era, every tongue has a `soundChangeRate` chance of a **sound change** (`applySoundChange` picks a phoneme and systematically shifts it via a plausible-shift table — voicing/lenition/vowel raising, e.g. *i→e* merger), and every culture's value axes take a small **random-walk drift damped by `cohesion`** (`driftValues`). Sound changes emit a `culture`-kind feed event (◈) **and a Chronicle legend** ("The Old Vant tongue shifted…") so the player *sees* the evolution (D27). New config `evolutionIntervalDays`/`valueDriftPerEra`; soak prints `drifts`.
- State: **Green.** 234 tests (was 228; +6 `evolution.test.ts`: sound change replaces a phoneme / determinism, value drift damped-by-cohesion + clamp, schedule once-per-era, and an end-to-end "languages accumulate changes + cultures drift, identically per seed"). `tsc` clean; 40k soak **0 violations**, pop→cap, `drifts=3` (tongues shifted 3×) while stable. Verified live (seed 2, 30k steps): Old Vant lost **'i'** (merged to 'e') — vowels went `[a,i,u,e,o] → [a,u,e,o]`; the Chronicle holds "The Old Vant tongue shifted: 'i' became 'e'"; Vant-kin's communal drifted 0.75→0.80; no console errors.
- State note: the EvolutionSystem draws from the sim RNG (shifts the trajectory), but no demographic test needed retargeting this time (reproduction seed 8 still surfaces a grandchild).
- Next: **M7 slice 4 — divergence → families & schism**: when a population splits (or a sub-group drifts far / cohesion drops), **fork** a daughter language+culture that then drifts independently → a family tree; the Chronicle records the schism and the lost/parent tongue. Then slice 5 (compression of dead tongues + the legibility lens — a "lineages of tongues & cultures" view).
- Blockers/Questions: none. Note: event-driven value response (famine→thrift, war→militarism per the design doc) is deferred until those events exist (famine signal / M8 conflict); for now drift is the cohesion-damped random walk.
- Decisions logged: none new (slice 3 executes the M7 plan + D25–D27).

## Session 21 — 2026-06-16 — M7 slice 2 COMPLETE: the causal culture model (D26 lands)
- Did: Built the seed **culture** layer and wired the first *causal* coupling. New `cultures` content type (Zod: 4 value axes communal/martial/traditional/open each 0..1, practices, cohesion, language ref; loader cross-checks the language exists). Two seed cultures: **Vant-kin** (old_vant, communal 0.75, mercantile, open-ish) and **Drakhan Clans** (drakhan, communal 0.35, martial, insular). A runtime **`CultureStore`** singleton (`src/culture/cultureStore.ts`) seeded (deep-cloned) from content — the shared-objects home that grows daughter cultures in slice 4. Agents now carry a `cultureId` (optional `Agent` field): **founders take their species' culture**, **children inherit the mother's** (culture = upbringing; surname stays father's). **Causal coupling (D26), first axis:** `wealthGoalFactor(values)` = 1.3 − 0.6·communal (bounded [0.7,1.3]) scales each agent's rolled wealth goal at spawn — so communal folk aim for less wealth. Inspector gained a **Culture** section (name + the four value axes as bars + practices). Soak prints a `cultures` count.
- State: **Green.** 228 tests (was 221; +7 `culture.test.ts`: schema/load + lang ref, store seeding/independence, `cultureForLanguage`, `wealthGoalFactor` monotonic+bounded, **the causal end-to-end test** (communal culture → lower mean wealth goal in a real town), founders-have-culture, child-inherits-mother's-culture). `tsc` clean; 40k soak **0 violations**, pop→cap, `cultures=2` persist. **One test retargeted:** the culture wealth-goal bias shifts the RNG trajectory (changed wealth goals → changed work/wander → different RNG draws), so `multi-generation stability`'s seed 11 stopped surfacing a grandchild in 40k ticks; moved it to the proven soak seed **8** (grandchild appears) — same kind of seed re-pick we've done before on RNG shifts. Verified live: inspector shows "Vant-kin · Communal 75% · Martial 30% …"; **Vant-kin mean wealth goal 61 vs Drakhan Clans 78** (causal effect visible); no console errors.
- Next: **M7 slice 3 — the evolution engine**: a generational scheduled pass that drifts a language (probabilistic sound change → a daughter) and drifts cultural values (slowly + in response to events), resisted by `cohesion`. Deterministic, procedural, off the hot path. Then slice 4 (divergence → families & schism), slice 5 (compression + the legibility lens).
- Blockers/Questions: none. Note: only the `communal` axis is causal so far (D26 "small + soak-gated"); martial/traditional/open are stored & shown but not yet wired to behaviour — they come online as conflict/innovation/contact systems land.
- Decisions logged: none new (slice 2 executes D26, recorded S19).

## Session 20 — 2026-06-16 — M7 slice 1 COMPLETE: language-derived naming wired into agents
- Did: Finished slice 1 by wiring the language model (built S19) into actual folk. Species schema swapped `nameSounds` → **`language`** (a tongue id); `human` → `old_vant` (soft), `dwarf` → `drakhan` (hard). `spawnAgent` now takes `content`, resolves the species' tongue, and builds the name as **given + surname** via `src/lang/language.ts` — keyed by entity id (regenerates identically, **consumes no sim RNG**). **Surnames inherit patrilineally**: a child takes the father's `surname` (new optional `Agent.surname`); founders coin their own. Full names now display everywhere (`Agent.name` = "Given Surname"). **Legibility (D27, first step):** the inspector shows each person's **Tongue**, and the two tongues are audibly distinct on the map (humans get soft Old-Vant names, dwarves hard hyphenated Drakhan names). Retired the M1 placeholder namer: deleted `src/content/names.ts` + `test/names.test.ts`.
- State: **Green.** 221 tests (was 219 pre-slice-1; +6 `language.test.ts`, −3 deleted `names.test.ts`, content.test fixture updated). `tsc` clean. The new namer **shifts the RNG trajectory** (the old `generateName` consumed variable RNG; the id-keyed namer consumes none), so per-seed worlds differ from before — still deterministic & stable: 40k soak (seed 8) **0 violations**, pop→cap(60), now ~46 human / 14 dwarf; multi-gen stability + identical-trajectory tests pass. Verified live (default seed, 6k steps): Old Vant names ("Suev Resmu", "Vuonit Elvi") vs Drakhan ("Thugktho Khor-baz", "Ragrba Drka-kug"); a child ("Mampurum Usni") inherits the father's surname ("Ramus **Usni**"); inspector shows Tongue; no console errors.
- Next: **M7 slice 2 — the seed CULTURE model (causal, D26)**: a `cultures` content type (value axes, practices, cohesion, language ref); cultures are shared objects agents reference; **value axes bias behaviour** (start small + soak-gated — e.g. communal↔individual → sharing / wealth-goal), surfaced in the inspector + a culture panel.
- Blockers/Questions: none. Note: surname inheritance is patrilineal and crosses tongues in mixed marriages (a human child of a dwarf father could carry a Drakhan surname) — acceptable/realistic for now; cultures (slice 2) can refine.
- Decisions logged: none new (slice 1 executes D25–D27 already recorded S19).

## Session 19 — 2026-06-15 — M7 planning: the "two loops" thesis + six decisions recorded (directed)
- Did: A design conversation before M7, not code. Laid out the thesis — Omnia has a strong, rigorous *substrate* but a *thin, decorative* life on top; the fix for both science and art is to **close two loops**: (1) cognition→consequence (make the inner life *causal*), (2) mechanism→meaning (make emergence *measurable* and the verisimilitude machinery *visible*). The five forks were answered decisively, with a request that **all of it be recorded before building**. Recorded: **VISION.md** gained a "guiding thesis" section + legibility/observe-first/live-model notes; **DECISIONS D25–D30** — D25 (two-loops thesis), **D26 (culture/values CAUSALLY steer behaviour, deterministically — refines, doesn't reverse, D19/D20: procedural cognition is causal, LLM *generation* stays recorded/off-trajectory)**, D27 (legibility is first-class — the player must SEE the evolution, not just a buried tree), D28 (wire the live Ollama model as an opt-in mode; stub stays default), D29 (a Science & Instrumentation track — measure distributions/networks/phase-transitions, export/diff runs), D30 (observe-first now; design for a future god-sim fork, don't build it); **ROADMAP** — M7 rewritten into 5 slices with causal culture + a legibility lens woven in, plus two new milestones (**7.5 Live Model Integration**, **7.7 Science & Instrumentation**) and a god-sim backlog entry.
- State: **Green** (219 tests; docs-only session, no code yet). Baseline confirmed before planning.
- Next: **M7 slice 1** — the seed **language model** (a `languages` content type: phonemes / syllable shapes / name patterns / light morphology / sound-change rate) + a deterministic, on-demand word/name generator (regenerates identically from rules+seed; no stored lexicons), then retire the M1 placeholder namer so names sound like their tongue. Then slice 2 (causal culture), slice 3 (evolution), slice 4 (divergence/schism), slice 5 (compression + the legibility lens).
- Blockers/Questions: none — all five forks answered (yes to causal culture, yes legibility-first, yes live model, yes science track, observe-first with a possible god-sim fork later).
- Decisions logged: **D25, D26, D27, D28, D29, D30**.

## Session 18 — 2026-06-15 — M6.5 COMPLETE: slice 3 — hotkey dashboards (economy / directory / family tree)
- Did: Final M6.5 slice. New shared modal base (`render/modalPanel.ts`: centred scrollable card + close ✕ + `bar`/`SECTION` helpers) drives three **live, mutually-exclusive dashboards**: **Economy** (`E`) — wealth distribution (median/mean/range/Gini/in-debt metric cards), employment (folk/adults/employed/mages), and a businesses table (trade · staff/cap · balance · wage, full ones flagged green, magical ✦); **Directory** (`F`) — a searchable roster of every soul (persistent input + live-filtered list), each row click → inspect **and jump the camera** (`renderer.centerOn`, new); **Family tree** (`T`) — a four-generation lineage of the inspected person (grandparents → parents → self+partner → children), resolving the **dead through their tombstones** (`Tombstone.parents/partner/children`), with clickable chips to re-root and browse the line. Wiring in `main.ts`: a `toggleDashboard` manager (one open at a time), `E/F/T` hotkeys with an input-focus guard (so typing in search/seed doesn't fire them), Esc backs out of a dashboard before pausing, and the loop refreshes the open dashboard (throttled). Added a basic **Settings** (seed) to the pause menu (`menu.showSettings`). Charts already live in Legends (C), so no separate charts panel.
- State: **Green.** 219 tests still pass (render-only changes — dashboards aren't import-tested); `tsc` clean. Verified live (seed 2, ~4k steps): economy lists all businesses + wealth/employment; directory lists all 31 folk and filters (31→1 on a name query); row-click inspects; family tree shows parents/children with clickable relatives; dashboards are mutually exclusive; Esc backs out; **no console errors**; screenshot confirms a clean Economy panel over the readable map. **M6.5 done** (slices 1 icons+camera, 2 menus, 3 dashboards).
- Next: resume **Milestone 7 — Culture & Language** (seed-YAML language model: phonology/phonotactics/on-demand lexicon/light morphology; then generational evolution; culture value-axes + schism; language-derived naming replacing the M1 placeholder; dead tongues → descent-trees). Backlog also still holds the deferred M5 async-Ollama wiring.
- Blockers/Questions: none.
- Decisions logged: none new (slice 3 follows the established overlay-panel patterns; no architectural reversal).

## Session 17 — 2026-06-15 — M6.5 slice 2 (menus) + collision, anti-freeze & minimizable panels (requested)
- Did: Human approved slice 1 and asked, in one go, to (a) fix folk "stuck in sleep/work loops, just sitting there", (b) stop agents/animals overlapping (collision), (c) make the always-on UIs minimizable, and (d) build slice 2 (menus). **Root-caused the stuck bug**: coworkers piled on the employer tile and kept each other's social need topped up (same-tile interaction) → froze forever. **Collision**: a shared `Occupancy` (count-per-tile) in `movementUtil`; `stepToward`/`wanderStep` won't step onto an occupied tile and update it as they move; `MovementSystem` + `FaunaSystem` build it from world state each pass → **no two mobiles (folk/fauna) share a tile** (new soak invariant). **Social/dialogue → adjacency** (8-neighbourhood) in `SocialSystem` + the `AISystem` dialogue pass, so company still meets the need under collision. **Work fidget** (~30%/tick at the workplace) so employed folk look busy not frozen. **Minimizable panels**: shared `panelUtil.makePanel` (collapsible header caret); `legend` + `eventFeed` refactored onto it. **Menus** (slice 2): `render/menu.ts` — start menu (seed input + how-to-play) and pause menu (resume / restart-same-seed / quit-to-menu); `main.ts` rewritten as a `menu`/`running`/`paused` state machine that (re)creates the sim on demand (Esc toggles pause).
- State: **Green.** 219 tests (was 212; +7 in `collision.test.ts`: Occupancy, movement-respects-occupancy, live 2-folk no-overlap, buildOccupancy, adjacency social). `tsc` clean. **Multi-generation stability + properties (identical-trajectory) + 10k soak all still pass** despite the movement changes. 40k soak (seed 8): **0 violations** incl. the new collision invariant, pop → cap. Verified live: start menu → boots a run; 27 folk + 57 fauna with **0 tile overlaps**; 20/27 folk moved over 30 ticks (no freezing); legend + feed minimize on header-click; Esc → pause menu → Resume; screenshot confirms clean, non-overlapping, readable map with a visible mage.
- Next: **M6.5 slice 3** — hotkey dashboards (population/charts, economy, relationships/**family tree**, search) + a settings panel. Then resume **M7 — Culture & Language**.
- Blockers/Questions: none. Note: collision/fidget shifted the deterministic trajectory (a blocked step now wanders → different RNG draws), so per-seed runs differ from pre-S17 — still fully deterministic and stable.
- Decisions logged: **D24** (mobile collision; same-tile→adjacency social; work fidget).

## Session 16 — 2026-06-15 — Milestone 6.5 (slice 1): readability core — category-first icons + camera (requested)
- Did: M7 was paused for a visual/UI overhaul (readability before lo-fi polish; the per-race outlines were invisible). Agreed a new milestone **M6.5** and, after a design pass (mocked the icon set as an SVG for sign-off), built **slice 1: the readability core**. **Category-first icon set** (`render/icons.ts`, single source of truth): dual-coded **shape + accent colour** per category so it reads when tiny — one **folk** icon (races not distinguished,), distinct **animal / plant / ore / timber / crystal / building** icons (resource kind chosen by `resourceIcon(typeId)`), folk **state badges** (mage ✦ / ill ✚ / action-glyph / child = smaller), and a **dormant hostile** slot (dashed red, reserved for M8). Rewrote the canvas `Renderer` to draw these with a **camera**: `setTransform`-based zoom/pan, wheel-zoom toward the cursor, drag + arrow-key pan, `+/−` zoom, clamped to the map (centres when fully zoomed out); **click-to-inspect inverts the camera transform**. New on-screen **legend key** (`render/legend.ts`, toggle `L`). HUD shows zoom %. Updated index.html/README controls.
- State: **Green.** 212 tests still pass (render layer isn't covered by tests — no sim changes); `tsc` clean. Verified live (canvas pixel-sampling + a real screenshot, which worked this time): folk render gold, animals amber, plants/buildings/resources in their colours; **click-to-inspect hits the right entity through the transform**; **zoom** changes the view and **pan** works when zoomed and **clamps** at the edges (and to centre at fit-zoom); legend panel present; no console errors. The map is markedly more legible than the old silhouettes.
- Next: **M6.5 slice 2** — start menu (new sim w/ seed, settings, help) + in-game **pause menu**. Then **slice 3** — hotkey dashboards (charts / economy / relationships-family-tree / search). Then resume **M7 — Culture & Language**.
- Blockers/Questions: none. Notes: buildings/plants/resources use their **content** colours on the map (category via shape), while the legend shows a representative accent — intentional. Graves aren't on the map (tombstones strip `Position`, D16) — noted as a future tweak. Decisions reversed: **D18's per-race silhouettes/outline rendering is superseded** (see D23).
- Decisions logged: **D23** (category-first dual-coded icons + camera; folk not race-differentiated; supersedes D18's silhouette rendering).

## Session 15 — 2026-06-15 — Milestone 6 COMPLETE: tiered Chronicle, statistical strata & the Legends view
- Did: Closed out M6 items 2 & 3. **Tiered Chronicle:** `chronicleAdd` now gates on `chronicleImportanceThreshold` (only notable events become legends; the daily ticker stays the EventLog), and a new scheduled **`HistorySystem`** compresses it (`consolidateChronicle`) the same way memory compresses — recent legends sharp, oldest rolled into one-line **eras** where importance ≥ `chronicleLegendImportance` (the founding cataclysm, 1.0) survives **by name** and ordinary births/weddings/deaths dissolve into a per-kind tally; eras merge past `chronicleMaxEras`. Chronicle entries gained a `kind` for the tallies. **Statistical strata** (`history/stats.ts`, `WorldStats` singleton): yearly bounded time-series (population, births, deaths, marriages, mages, Gini, median wealth, avg age) + a cumulative cause-of-death histogram, all **derived from durable state** (living agents + persistent tombstones) so it couples to nothing and uses no RNG. **Legends view** (`legendsPanel.ts`, replaces the minimal chronicle panel, still the C key): reads as a story — recent named legends, an "Ages past" section of compressed eras, then "the town in numbers" lo-fi SVG sparklines + a cause-of-death breakdown. `HistorySystem` wired after `MemorySystem`; `WorldStats` created at world-gen.
- State: **Green.** 212 tests (was 203; +9: chronicle gate/summarizeEra/consolidate/era-merge, sampleStats derivation + bound, HistorySystem schedule + throttle, end-to-end loop integration). `tsc` clean; coverage 99.19%/93.85% (chronicle 100% stmts, stats 100% stmts, HistorySystem 100% stmts). Soak 40k ticks (seed 8): **0 violations**; new bounded invariants (eras ≤ maxEras, samples ≤ maxStatSamples) + metrics — `eras` climbs 0→3 (compression fires at default balance), `samples`→38, all bounded. Verified live (default seed, ~28k steps): Legends opens with named recent legends + an "Ages past" era preserving the founding cataclysm by name ("…came the Ashfall… and 13 births, 1 death besides") + population/wealth/Gini/births sparklines + cause-of-death bars; no console errors.
- **M0–M5 audit (requested — "ensure all previous milestones are fully finished"):** every M0–M6 roadmap item is now `[x]`. M0–M4 DoDs hold (102→212 tests green, 40k-tick soak 0 violations); M5 completed last session (dialogue/dreams/decisions, part 2); M6 complete this session. M3's deferred sub-items (supply/demand market, resource-consuming crafting, skill gating) were always explicitly **backlogged/M8**, not part of M3's DoD — so no milestone is half-done. Known non-DoD gap still backlogged: the YAML config loader (`src/sim/config.ts` is authoritative; `docs/simulation.yaml` is reference-only).
- Next: **Milestone 7 — Culture & Language** (phonology/lexicon language model from seed YAML; generational sound-change/drift/divergence; culture value-axes + schism; language-derived naming replacing the M1 placeholder; dead tongues compress to descent-trees). Backlog also still holds the deferred M5 async-Ollama wiring.
- Blockers/Questions: none. (Note: `medianWealth` in the strata is **net worth** and can read negative when many young/indebted agents drag the median below zero — accurate, not a bug.)
- Decisions logged: **D22** (tiered Chronicle like memory; strata derived from durable state, not event counters).

## Session 14 — 2026-06-15 — Milestone 6 (part 1): multi-resolution memory rollups & pruning
- Did: First M6 item — the per-agent compression pipeline (SIMULATION_MODEL Mechanisms 1–2). `remember()` is now **pure-append** (no more naive drop-the-oldest, which flattens the story — D4); a new scheduled **`MemorySystem`** is the sole authority that bounds an agent's stream. When the working set exceeds `workingMemorySize`, it rolls the oldest events down into bounded **episodic summaries** (new pure `ai/consolidation.ts`): high-importance events stay **named/vivid** in the digest, trivia dissolves into a count ("and N quieter days"), raw events are discarded, and once over `maxSummaries` the two oldest **merge** into a coarser era (the more notable keeps its words). Three shrinking resolutions per agent: raw working memory → episodic summaries → beliefs (M5). Deterministic, **no RNG**, off the hot path (runs every `memoryRollupIntervalDays`). Inspector's Mind now shows summaries (❧) and counts compressed memories; new config knobs (rollup interval / retain / threshold / maxSummaries). Dropped the now-unused `cap` arg from `remember()` (8 call sites).
- State: **Green.** 203 tests (was 190; +13 in `memory.test.ts`: summarizeBlock naming/folding/cap, mergeSummaries coarsening, consolidateMemory no-op/rollup/merge-bound, remember pure-append, MemorySystem schedule/interval/bounded, **plus an end-to-end loop-integration test** proving the rollup fires through `runTicks` and stays bounded). `tsc` clean; coverage 99.09%/94.12% (consolidation 100%, MemorySystem 100% stmts). Soak 40k ticks (seed 8): **0 violations**, pop holds at cap; new `summ` metric + bounded-summaries invariant. Verified live (default seed, 3k steps): inspector Mind renders, no console errors.
- Honest note: at the **current** balance, memory accrues slowly (births/weddings/illness/bereavement/work), so no agent exceeds `workingMemorySize=30` in the default soak → `summ=0` there. The rollup is therefore a *tested, bounded safety mechanism* today, proven to fire end-to-end via the integration test (small working set) and the unit tests; it'll do real work once per-agent memory volume grows (conversation logs, daily activity) in later milestones. Deliberately did **not** inflate memory just to trigger it (avoids gratuitous balance change).
- Next: **M6 item 2** — the tiered **Chronicle** (notable events only, recent sharp / ancient compressed to one-line legends) + **statistical strata** (fixed-size running aggregates feeding world-health charts). Then the **Legends view** UI (item 3).
- Blockers/Questions: none.
- Decisions logged: **D21** (pure-append `remember()`; scheduled `MemorySystem` as sole bounding authority; multi-resolution episodic summaries).

## Session 13 — 2026-06-15 — Milestone 5 COMPLETE: dialogue, dreams & major-decision prompting
- Did: Second half of M5 — gave the soul a voice, built on part 1's machinery and honouring the same invariants (determinism, never-block-the-tick, recorded replay). Three new "soul" outputs, all inside `AISystem` on the **synchronous deterministic `completeSync` path** (consumes **no RNG**, never blocks): **dreams** (a sleeping agent at night dreams an image from its memories), **dialogue** (two agents who share a tile *and* a bond — partner/friend — exchange a themed line; speaker chosen by id; one conversation per tile), and **decisions** (a fresh *turning-point* memory — wedding/birth/bereavement, importance ≥ `decisionImportance` 0.65 — prompts a first-person resolution; narrates a juncture the sim already decided, so generation stays out of the trajectory). New `Utterance` on `Memory` (bounded `maxUtterances`); rarity from a shared per-tick budget (`maxExpressionsPerTick`) + per-agent interval (`expressionIntervalDays`) with separate `lastSpokeTick`/`lastDreamTick` timers. Stub gained three themed registers (dream/say/resolve) selected by a cue word the new prompt builders embed (`buildDreamPrompt`/`buildDialoguePrompt`/`buildDecisionPrompt`). Everything **recorded** to `AIRecord` for exact replay. Event feed gained `dialogue`/`dream`/`decide` kinds (❝/☾/➜); inspector's Mind section now lists recent utterances. **Pure flavour — nothing feeds back into behaviour.**
- State: **Green.** 190 tests (was 179; +11 in `soul2.test.ts`: stub registers, dream pass + day/awake guards, decision pass + mundane-skip, dialogue pass + stranger-skip, budget cap, utterance bound, determinism; plus the replay test now also asserts **utterances** reproduce). `tsc` clean; coverage 99.06%/93.92% (AISystem 100% stmts); soak 40k ticks (seed 8) **0 violations**, pop→cap(60) holds, `reflective` 7→33, `utters` climbs 47→287 and stays bounded. Verified live (default seed 2, 8k steps via `__omnia.step`): feed shows decide/dialogue/dream lines, believably themed ("Beraelrath swore to give their children a better life.", "Jetuwen to Hingesmial: 'How are the little ones?'", "…dreamed their children grown and well"); inspector Mind renders utterances with glyphs; **no console errors** (screenshots still time out in this preview env, as before).
- Next: **Milestone 6 — History & Legends** (importance scoring + scheduled rollups/pruning of memory; the tiered Chronicle + statistical strata; a Legends view UI). M5's deferred async live-model wiring (drive Ollama via `AIRunner` across ticks) is on the backlog — the seam, runner & recording are ready; only the live wiring + a results-drain remain.
- Blockers/Questions: none. Note: live dialogue/dreams/decisions use the deterministic **stub** (no Ollama needed); switching to a real model is the deferred async-wiring task, not a code change to these features.
- Decisions logged: **D20** (dialogue/dreams/decisions on the deterministic recorded flavour-only path; major-decision prompting narrates an already-decided juncture).

## Session 12 — 2026-06-14 — Milestone 5 (part 1): the Soul — memory, reflection & recorded replay
- Did: First half of M5, the LLM layer, built to honour determinism + never-block-the-tick. **AIProvider** seam (`src/ai/`): a deterministic offline **StubProvider** (default — synthesises a belief from the themes in a reflection prompt; same prompt → same belief), an opt-in async **OllamaProvider** (OpenAI-compatible), and an **AIRunner** (bounded concurrency + per-call timeout fallback) for the async path off the hot path. **Memory** component + `remember()`; life systems now record memories (births, weddings, illness, bereavement, taking work) with importance. **Retrieval** by recency×importance×relevance (deterministic hash embeddings). **AISystem**: throttled reflection (per-agent interval + global cap) → beliefs, via the synchronous `completeSync` path only (so it never blocks the tick and consumes no RNG → determinism intact). **Recording/replay**: every response logged to an `AIRecord` singleton; `RecordedProvider` replays; a test proves a *non-deterministic* run replays identically. UI: inspector shows a "Mind" section (beliefs + recent memories); event feed shows reflections (☼).
- State: **Green.** 179 tests (was 159; +20: provider/stub/memory/recording, AISystem reflection+throttle+cap+determinism, recorded-replay, AIRunner concurrency/timeout). `tsc` clean; coverage ~99% (now gating src/ai). build green; soak 40k ticks 0 violations, `reflective` agents climb 7→33, world stable. Verified live (seed 2): beliefs reflect real memories ("treasures family above all"), feed shows reflections, no console errors.
- Next: **M5 part 2** — LLM-driven dialogue at meaningful moments, major-decision prompting, dreams; wire the async AIRunner/Ollama path into the live loop (results applied across ticks, recorded). Then Milestone 6 (History & Legends / compression).
- Blockers/Questions: none. Notes: the live app + tests use the deterministic **stub** (no Ollama needed); switch `llm.provider` to `ollama` for a real model (async path wiring is part 2). Beliefs don't yet influence behaviour (keeps generation out of the deterministic trajectory).
- Decisions logged: **D19** (deterministic stub default; synchronous replayable reflection; async+recorded live model).
- **Audit (M0–M5), requested:** every `[x]` roadmap item has matching code (full system roster + 7 content types present & validated); determinism invariant holds (no `Math.random` in `src`); no TODO/FIXME/placeholders; CLAUDE.md commands == package scripts; CI runs lint+coverage+soak; 179 tests green, ~99% coverage, soak 0 violations, CI green on main. **One real gap found & fixed:** the README told users to edit `config/simulation.yaml` to change the seed, but that file (`docs/simulation.yaml`) is **not loaded** — `src/sim/config.ts` is authoritative. Corrected the README + the config comment; added "wire a YAML config loader" to the backlog. Deferred items (market, crafting, dialogue/decisions/dreams, trade/building/conflict, lost arts, family-tree UI) are honestly marked, not falsely claimed.

## Session 11 — 2026-06-14 — Visual vocabulary, live event feed & resource gathering (requested)
- Did: Requested — a clearer visual vocabulary, a live drama feed, and more to actually *do* (chose "overhaul + one interaction: resource gathering"). **Visuals:** rewrote the renderer with a distinct silhouette per class — folk = pawn (head+body; body=species colour, outline=action, smaller=child, violet pip=mage), fauna = triangle, flora = sprout (stem+leaf), resource = block, business = house. **Live feed:** new `EventLog` ring-buffer singleton (`src/history/eventlog.ts`) + `emitEvent`; systems emit births, deaths (w/ cause), marriages, hirings, illnesses, magic casts, exhausted veins; a fixed "Town Happenings" panel (`eventFeed.ts`) streams them. Distinct from the Chronicle (legends). **Resource gathering:** professions can `gathers` a resource (miner→ore finite, laborer→timber renewable, validated at load); gatherers walk to nodes; new `GatherSystem` depletes them; a finite vein that runs dry is destroyed + logged. HUD reworked (Year/Folk/Mages/Graves/Fauna/Gini). Dev `__omnia` handle gained `eventFeed` + `step`.
- State: **Green.** 159 tests (was 150) across 19 files (eventlog 4, gathering 5); `tsc` clean; coverage ~99%; build green; soak 40k ticks 0 violations (added nodes metric). Verified live via pixel inspection (screenshots time out in this preview env): pawns/triangles/blocks/mage-pips/action-outlines all render; the event feed shows births/deaths/magic with glyphs; no console errors.
- Next: continue the interaction list — **trade/market** is the next pick (then building, then conflict/vice); the planned Milestone 5 (LLM soul) is still queued after.
- Blockers/Questions: none. Note: resource gathering currently has no economic consequence (wages unaffected by depletion) — coupling is roadmapped. Screenshots can't be captured in this preview environment, so visuals were verified by canvas pixel sampling.
- Decisions logged: **D18** (UI brought forward by request: per-class shapes + live event feed; resource gathering as first new interaction).

## Session 10 — 2026-06-14 — Milestone 4 COMPLETE: reproduction & the first generations
- Did: Second half of M4 — closed the generational loop. Extracted a shared `spawnAgent` (founders + newborns use one path). New `ReproductionSystem`: a married, opposite-sex, fertile, fed, healthy couple off cooldown conceives a child wired into both parents' `Lineage`; births **pause at `maxPopulation`** so the town holds a carrying capacity. Magic aptitude is **heritable** (a mage's child: 25% vs the species base). Founders are pre-paired into households at world-gen. Reworked marriage from lossy spatial-only courtship into a reliable **matchmaking pass** in `SocialSystem` (unattached adults pair, with an incest guard) — the spatial-only version left the town slowly dying out. Renderer draws children smaller than adults (visible generations).
- Tuning (via soak): purely spatial courtship → population slow-collapsed to single digits; switched to matchmaking + pre-paired founders + birth/fertility tuning → population now **climbs to the cap (60) and holds across 100+ sim-years**; grandchildren appear by ~yr 18.
- State: **Green.** 150 tests (was 145) across 17 files; `tsc` clean; coverage 99%; build succeeds. Standalone soak (40k ticks ≈ 42 yr, seed 8): pop→cap, born 55, graves 17, mages 2→5 (heritable), **0 violations**. New `multi-generation stability` test asserts bounded population (never 0, never > cap), tombstones accrue, and a 3-deep lineage (grandchild) forms. Verified live (seed 2): births happen, inspector shows a parent's partner + children, no console errors.
- Next: **Milestone 5 — The Soul (LLM layer)**: `AIProvider` + Ollama (concurrency cap, timeout fallback); memory stream + retrieval; reflection into beliefs; LLM dialogue at meaningful moments; record LLM responses into the event log for deterministic replay. Never block the tick on the model.
- Blockers/Questions: none. Notes: wealth Gini reads very high (~0.9) now because the many penniless children are counted — cosmetic, economy still bounded; could measure adults-only later. Soak seed 8 / default seed 2 still contain mages after the RNG shifts.
- Decisions logged: **D17** (carrying-capacity reproduction; matchmaking-pass marriage; heritable aptitude; pre-paired founders).

## Session 9 — 2026-06-14 — Milestone 4 (part 1): mortality, relationships & tombstones
- Did: First half of M4 — the life cycle's social + ending halves. **Identity:** agents gain `sex`, an age (reused `ticksAlive`), and a rolled `lifespanTicks`; founders seeded with a spread of ages (16–55) so the town starts mixed. Time compressed via `daysPerYear` (config) so lives turn over. **Needs:** added `social`. **New components:** `Health`, `Relationships` (edge map: sentiment+type), `Lineage` (partner/parents/children), `Tombstone`. **Systems:** `SocialSystem` (social decays, proximity interactions warm sentiment → friend edges, adults court → marry, recorded to the Chronicle) and `HealthSystem` (illness + age-ramped mortality → death). **Death** (`src/sim/death.ts`): strips an agent's living components and attaches a compact `Tombstone` to the *same entity id*, widowing the partner — lineage pointers keep resolving. ActionSystem: new `socialize` action (seek company when lonely); only adults work. MovementSystem: walk toward the nearest person to socialise. EconomySystem: don't hire children. Renderer HUD shows Graves; inspector shows sex/age, social/health bars, and family (partner + living children).
- State: **Green.** 145 tests (was 136) across 16 files; `tsc` clean; coverage 98.95%; build succeeds; soak 10k ticks (~10 sim-years), **0 violations** — marriages form (~12–14 wed), tombstones accrue (graves climb), avg age rises. Verified live (default seed → seed 2, has a mage): inspector shows the new life-cycle facts incl. a named partner; no console errors. New invariants in soak: social ∈ [0,1], health ∈ [0,1].
- Next: **M4 part 2** — reproduction (partnered adults → children), `Lineage` parent/child links populated, and **multi-generation population balance** (births ≈ deaths over deep time) — the DoD's "sustains across generations." Family-tree view is M6/M8.
- Blockers/Questions: none. Note: in part 1 (no births) population only *declines* — that's expected; part 2 closes the loop. Default seed changed 1→2 and soak seed 7→11 because the new RNG draws (sex/age/lifespan before the magic roll) shifted which seeds contain a mage.
- Decisions logged: **D16** (strip-components-keep-id tombstones; `daysPerYear` time compression; proximity relationships).

## Session 8 — 2026-06-13 — Milestone 3 COMPLETE: the unified Capability system (technology + magic)
- Did: Second half of M3. Generalised the M1 forage proof into the **unified capability engine** (`src/capability/invoke.ts`): `canInvoke` + `invokeCapability` check prerequisites (aptitude) → cost (mana/energy) → apply effect tags; returns success. One code path serves both traditions. Extended `CapabilitySchema` with `prerequisites.aptitude` + `cost.{mana,energy}`; added effect tag `restore_energy`. **Magic:** rare innate aptitude rolled per agent at spawn (per species `magicAptitudeChance`) → optional `Magic` component (mana pool) only on the gifted; `CapabilitySystem` regenerates mana and lets mages cast `conjure_meal`/`mend_vigor` to cover their own needs (throttled by slow mana regen). **Magical profession:** `hedge_witch` (requiresAptitude) — EconomySystem hires only the gifted into magical businesses, and a mage prefers magical work (becomes the town's hedge-witch). **Technology:** `forage` is the common ungated tech capability. Renderer: violet pip on mages + HUD Mages count; inspector shows aptitude + mana bar. Soak: mage count + mana-bounds invariant; standalone soak seed → 7 (has a mage). Added a dev-only `step()` to `window.__omnia` for manual stepping.
- State: **Green.** 136 tests (was 117) across 15 files; `tsc` clean; coverage 98.6%; build succeeds; soak 10k ticks 0 violations, stable, mages=1. Verified live (default seed 1 has 1 mage): the mage is hired as Hedge-Witch, renders with the violet pip, inspector shows mana; no console errors. (Live ticking needs a foreground tab — hidden tabs throttle rAF; verified via the new `step()`.)
- Next: **Milestone 4 — Relationships & Life Cycle** (relationship graph + SocialSystem + social need; courtship→marriage→reproduction→children→Lineage; aging; illness/injury/death + tombstones).
- Blockers/Questions: none. Magic is *deliberately* rare — a 20-person town often has 0–1 mages; the rarity is proven statistically in tests (~8/300). Deferred to backlog: supply/demand market, resource-consuming crafting, skill/knowledge gating, lost arts.
- Decisions logged: **D15** (capability engine: one invoke path; magic as an optional component; pay not gated on location; wealth-goal bounding).

## Session 7 — 2026-06-13 — Milestone 3 (part 1): the Economy
- Did: First half of M3 — a stable, stratified economy. New `professions` content type (Zod schema + 5: laborer/farmer/miner/artisan/merchant, varied wages). New components: `Job`, `Business` (employer org entity with balance/capacity/wage/revenue); `Wallet` gained `debt`; `Agent` gained a per-agent `wealthGoal`; new `work` action. New `EconomySystem`: hires unemployed agents into businesses with openings, pays wages (business balance → wallet) to anyone choosing to work, books business revenue, and charges a daily cost of living that becomes tracked debt when broke. Pure money helpers in `economy.ts` (earn pays debt first; spend never goes negative). Wealth-distribution metric in `wealth.ts` (min/median/mean/max net worth + Gini). ActionSystem: survival (hunger/energy) first, else work when employed & below wealth goal, else wander. MovementSystem: agents commute toward their employer (visual). World-gen places businesses + rolls wealth goals. Renderer draws business buildings + the work action + HUD wealth; inspector shows livelihood (job/gold/debt/goal) for folk and a business panel; clicking a business works.
- Tuning notes (two real bugs found & fixed via soak): (1) a high survival gate (0.7) starved earning time → ran the gate at `actionThreshold` (0.4); (2) gating pay on standing exactly on the employer tile let greedy pathfinding strand workers in **runaway unbounded debt** — changed to pay any employee who chooses to work (commute is now visual only), and lowered upkeep 4→3. Result: wealth bounded & stable (min~19/median~58/max~105, Gini~0.20, inDebt 0 in steady state) over 10k ticks.
- State: **Green.** 117 tests (was 102) across 14 files; `tsc` clean; coverage 98.5%; build succeeds; soak 10k ticks 0 violations (added M3 invariant: gold≥0, debt≥0). Verified live: 8 businesses, 20/20 employed, agent & business inspectors work, HUD shows median wealth/Gini, no console errors.
- Next: **M3 part 2** — the unified **Capability system** (invoke→prereq→cost→effect), **technology** tradition (knowledge-gated, crafting tied to resources) + **magic** tradition (rare, aptitude-gated; the `magicAptitudeChance` already in species data; mana). Generalises the M1 `forage` effect-tag proof.
- Blockers/Questions: none. Note: in the stable config nobody is *permanently* broke (good for "stable over many sim-years"); the debt mechanic + "go broke" path is exercised by unit tests rather than the default soak. The supply/demand **market** and bankruptcy were deferred to the backlog to keep this chunk bounded.
- Decisions logged: none new (economy follows D9 data-driven content; businesses-as-entities per roadmap). Pay-not-gated-on-arrival + wealth-goal-bounding recorded here for the next session.

## Session 6 — 2026-06-13 — UX: decouple playback speed + speed slider (requested)
- Did: The render loop was ticking the sim once per animation frame (~60 ticks/s), so agents teleported a cell 60×/s — too fast to click. Rewrote `main.ts` to advance the sim on a real-time accumulator at `cfg.simSpeedTicksPerSecond` (default 6), drawing every frame regardless (the "sim time decoupled from real time" principle in ARCHITECTURE). Added `src/render/controls.ts` (`SpeedControl`): a bottom speed slider (0–40 ticks/s) + ▶/⏸ button; Space toggles pause through it. No simulation/determinism changes — render layer only. README controls table + index.html hint updated; `simSpeedTicksPerSecond` added to SimConfig and aligned in simulation.yaml.
- State: **Green.** 102 tests pass; `tsc` clean. Verified live: tick rate is now exactly 6/s (was ~60), slider sets it (0 = paused, 20 = 20/s, confirmed by measuring the clock), control bar visible bottom-center.
- Next: **Milestone 3 — Economy, Work & Capabilities** (unchanged).
- Blockers/Questions: none.
- Decisions logged: none (playback speed is a renderer concern; follows ARCHITECTURE's sim/real-time decoupling). Idea for later: interpolate entity positions between ticks for smoother motion — added to ROADMAP backlog.

## Session 5 — 2026-06-12 — Milestone 2 COMPLETE: living world (flora, fauna, resources, brain tiers, Chronicle)
- Did: Finished M2. **Content:** added `flora` (4), `fauna` (2), `resources` (3) content types + Zod schemas; biome spawn tables (`flora`/`fauna`/`resources` weighted lists) with **load-time referential integrity** (a biome referencing an unknown id fails loud). **Systems (new ECS):** `FloraSystem` (grow toward maturity, spread to adjacent open passable tiles, bounded by maxFlora), `FaunaSystem` (instinct-only: graze ripe flora, breed when fed + off-cooldown, starve and die — bounded by maxFauna), `ResourceSystem` (renewables regrow, finite don't). Replaced the abstract "food" entity with real **flora**: agents forage ripe flora via the existing `forage` capability (boundary intact). **Brain tiers** formalized in `src/sim/tiers.ts` (sapient/fauna/none as mutually-exclusive component markers; LLM can only ever attach to sapient). **History:** `src/history/` Chronicle (append-only legend log, singleton component) + deterministic `generateBackstory` seeded from world composition; viewable in-app via a Chronicle panel (C key). **Render/UI:** renderer draws biomes→resources→flora→fauna→folk; polymorphic inspector now handles folk/fauna/flora/resource (click any of them); HUD shows Folk/Fauna/Flora counts. Extracted shared movement helpers (`movementUtil.ts`) used by both MovementSystem and FaunaSystem. Fixed a latent click-mapping bug (canvas CSS-scale factor) + made the page layout non-clipping. Added a dev-only `window.__omnia` debug handle (stripped from prod).
- State: **Green.** 102 tests (was 66) across 13 files; `tsc` clean; coverage 98.3% (now gating src/world + src/history); `npm run build` succeeds; soak 10k ticks ~1.0 s, **0 violations**, world stable (folk survive, fauna 10→150 to carrying capacity, flora to cap). Verified live in-browser: biomes/flora/fauna/resources render, click-to-inspect works for every entity type (confirmed deterministically), Chronicle shows the seeded backstory, no console errors.
- Next: **Milestone 3 — Economy, Work & Capabilities** (jobs/wages/EconomySystem; the unified Capability invoke engine; technology + magic traditions; wealth metric).
- Blockers/Questions: none. Note: the default world reaches a stable carrying capacity (flora/fauna pinned near caps) rather than oscillating — this satisfies the "runs stably" DoD; the "can crash" clause is demonstrated by a dedicated overgrazing test, not the default soak.
- Decisions logged: none new (flora/fauna/resources as data archetypes and brain tiers as component markers follow D9/D10). Recorded the tilemap-as-singleton and shared-movement-util choices here for the next session.

## Session 4 — 2026-06-09 — Milestone 2 (part 1): World substrate — biomes, terrain, tile grid
- Did: Took the foundational first slice of M2 (one coherent item per the guardrails). New `biomes` content type (Zod schema + 5 authored YAMLs: ashen_plains, fungal_forest, crystal_flats, irradiated_wastes, drowned_ruins/water). Dense `TileMap` singleton component (`src/world/tilemap.ts`, flat typed arrays, self-contained colour/passable/name look-ups so systems/renderer don't need the registry). Seeded **Voronoi world-gen** (`src/world/worldgen.ts`): scatter weighted biome seeds, each tile takes its nearest seed → contiguous regions, deterministic. `createSimulation` generates terrain first and places food + agents only on passable tiles. MovementSystem now respects passability (steps blocked by water; food-seekers route around it; wander stays on land) — reads the TileMap singleton from the world, so no signature churn. Renderer draws biome backgrounds + brighter inset food markers; inspector shows the agent's terrain. New M2 invariant (no agent on an impassable tile) added to the soak runner, the soak test, and the property test.
- State: **Green.** 80 tests pass (+14: world.test.ts has worldgen determinism, tile accessors, movement-around-walls, and a createSimulation integration check). `tsc` clean, coverage 97.7% (now gating src/world too), `npm run build` succeeds, soak 10k ticks in ~74 ms with 0 violations. Verified live in-browser: all 5 biome colours + food markers render, no console errors.
- Next: continue **Milestone 2** — **flora** (grow/spread/harvest), **fauna** (instinct-only light agents, no LLM), **resources** (renewable + finite), then formalize the **brain tiers**, then the post-apocalyptic **backstory Chronicle**. (These were intentionally deferred to keep this session to one coherent item.)
- Blockers/Questions: none. (Population still trends down — reproduction is M4.)
- Decisions logged: none new. (Voronoi terrain + dense-array tilemap are implementation choices, not architectural reversals; noted here so the next session knows terrain is a singleton component, not per-tile entities.)

## Session 3 — 2026-06-08 — Test hardening + Milestone 1 complete: Content Framework & Species
- Did (test infra, requested): added coverage reporting (`@vitest/coverage-v8`, gated 90% stmts/lines/funcs + 85% branches, scoped to src/sim+content+capability) wired into CI via `npm run test:coverage`; added `fast-check` property tests (RNG bounds/replay over arbitrary seeds, agents in-bounds + needs in [0,1] for any seed, seed→identical trajectory, ECS destroyed-never-queried). Property testing surfaced a float-rounding edge in a test assertion (fixed the test). Expanded README with a "sanity-checking it yourself" section.
- Did (M1): YAML loader + **Zod** schemas (schema-as-type, `.strict()` so typos fail) → immutable typed `Registry`. Pure `loadContent(Map<path,text>)` core with two sources: `fsSource.ts` (Node/tests/soak) and `import.meta.glob` (browser/main.ts). Two species authored purely in YAML (`content/species/human.yaml`, `dwarf.yaml`) with distinct needs multipliers, colours, sizes, spawn weights, and name-sound pools. Agents now spawn from weighted archetypes with generated names + a baked `Species` component. Per-species name generator (`names.ts`). First capability `content/capabilities/forage.yaml` (effect tag `restore_hunger`) wired end to end: data declares the tag, `src/capability/effects.ts` implements it, loader fails loud if a tag has no implementation; MovementSystem eats by invoking it. Renderer shows species via ring colour + dot radius; inspector shows species. Removed now-dead `foodRestoreAmount` config.
- State: **Green.** 66 tests pass, `tsc` clean, coverage 97.5% stmts / 95% branch, `npm run build` succeeds (YAML bundled), soak 10k ticks in ~59 ms with 0 violations. Verified the browser path live (canvas draws both species' rings; content loaded via glob; no console errors).
- Next: **Milestone 2 — World & Environment** (biomes, terrain/tile grid, flora/fauna/resources, formal brain tiers, seeded world-gen + backstory Chronicle).
- Blockers/Questions: none. (Population still trends down — reproduction is M4, as designed.)
- Decisions logged: none. (M1 followed D9/D11 directly. Note for the record: chose Zod for validation and the `yaml` package for parsing — both are the conventional TS choices implied by ARCHITECTURE/CONTENT_AND_DATA; not treated as new decisions.)

## Session 2 — 2026-06-08 — Milestone 0 complete: Foundation & Heartbeat

- Did: Scaffolded the full TypeScript project (Vite + Vitest + tsx). Hand-rolled ECS (World/EntityId/addComponent/query). Seeded Mulberry32 RNG — Math.random() banned from sim code. Headless fixed-timestep tick loop with deterministic system ordering (Clock → Hunger → Action → Movement). Tiny world: 20 agents on a 64×64 grid, `Position`, `Needs`, `Wallet`; 25 food sources; `MovementSystem`, `HungerSystem`, day/night clock. Utility action selector (seek_food > sleep > wander). Agents die when hunger bottoms. Canvas renderer: colored dots (white=wander, orange=food, blue=sleep) + HUD. Click-to-open inspector panel. 33 tests: 7 RNG, 9 ECS, 14 systems (ClockSystem, HungerSystem, ActionSystem, MovementSystem), 2 seed-determinism, 1 soak. GitHub Actions CI (lint + test + soak). Soak: 10,000 ticks in 37 ms, zero invariant violations, 4 of 20 agents alive at day 41. Filled in CLAUDE.md commands and README Run section.
- State: **Green.** All 33 tests pass, soak passes, tsc clean.
- Next: **Milestone 1 — Content Framework & Species** (YAML loader + schema validation, data-driven human archetype, second species, name generator, first capability tag).
- Blockers/Questions: Population drifts downward ~20→4 over 41 days (food regen outpaced by consumption with no reproduction). This is correct behavior for M0 (starvation works; no birth mechanic yet). If a stable population is preferred for demos, raise `foodRegenPerTick` in `src/sim/config.ts`.
- Decisions logged: none (all M0 choices follow D3/D5 directly).

## Session 1 — project founding — Flavor & content architecture defined
- Did: Added four design docs — `CONTENT_AND_DATA.md` (data-driven validated-YAML content), `WORLD_AND_ENVIRONMENT.md` (biomes/flora/fauna/resources + brain tiers), `MAGIC_AND_TECHNOLOGY.md` (unified capability system; magic & tech as traditions), `CULTURE_AND_LANGUAGE.md` (deep-but-tenable evolution + naming). Updated VISION (setting + deferred aesthetic), ARCHITECTURE (content layer, brain tiers), ROADMAP (reordered into M0–M8), DECISIONS (added D8–D13), README, and `config/simulation.yaml`. Still no code.
- State: Pre-code. Design foundation complete and internally consistent. Nothing to test yet.
- Next: Execute **Milestone 0 — Foundation & Heartbeat** (TS + ECS + seeded RNG + headless tick loop + first tests + ~20-agent world + minimal dot renderer). Stop and report after M0.
- Blockers/Questions: none.
- Decisions logged: D8–D13 (see DECISIONS.md).

## Session 0 — project founding — Foundation documents created
- Did: Wrote the initial document set (README, CLAUDE, VISION, ARCHITECTURE, SIMULATION_MODEL, ROADMAP, PROGRESS, DECISIONS) and `config/simulation.yaml`. No code.
- State: Pre-code. Repo held docs + config only.
- Next: Execute Milestone 0.
- Blockers/Questions: none.
- Decisions logged: D1–D7.
